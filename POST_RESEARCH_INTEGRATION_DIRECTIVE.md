# POST_RESEARCH_INTEGRATION_DIRECTIVE.md

Use this once, together with the five files in `research/final-2026-08-25/`.

## Task

The separate Inquiry Calculus research crawl is now finished for this handoff.

Do not restart the research and do not implement its chronology.

Reconcile the final research corpus against the **current** repository and weave only the surviving, implementation-relevant constraints into the existing implementation plan/frontier.

Authority order:

```text
canonical specification
> implementation plan / current frontier
> current code + tests + migrations + tool returns for actuality
> accepted decisions
> final research corpus for derived guidance / breakers
```

Historical repository heads or “current frontier” statements inside research files are ancestry, not present actuality.

## First actions

1. Read `AGENTS.md`, `PERSISTENT_CODEX_GOAL.md`, `PROJECT_RESEARCH_IMPLEMENTATION_HANDOFF.md`, `IMPLEMENTATION_FRONTIER.md`, `CONFORMANCE_STATUS.md`, `DECISIONS.jsonl`, `FAILURES.jsonl`, the comprehensive implementation plan, and all five final research files.
2. Probe current Git/code/tests/migrations before relying on any embedded repository coordinate.
3. Classify each relevant research result as `ALREADY_DEMONSTRATED`, `CURRENT_FIXTURE`, `PHASE_LOCAL_CONSTRAINT`, `FUTURE_OPTIMIZATION`, `SEMANTIC_REOPEN_CANDIDATE`, or `REJECTED_OVERCLAIM`.
4. Do not add a new semantic primitive, runtime opcode, database, scheduler, or crate merely because research names one.
5. Update stale control-document wording that says the research is still running or unavailable. Do not alter their substantive execution discipline without a breaker.

## Current observed coordinate

The handoff was prepared against:

```text
b7613699a06a9b2fc9b5c20e44020c5fbd15f1cd
feat: execute prepared mock probes
```

Verify this immediately; continue from newer actuality if it has advanced.

At that coordinate the live frontier is fresh-process cold replay from a committed actualized probe through:

```text
event/raw reload
-> finite decode
-> supported answer
-> capture-safe source binding
-> exact source/runtime lowering
-> admitted resumption
```

without pre-crash in-memory objects or redispatch.

Treat the replay need as the current breaker on whether source/runtime lowering can remain purely derived or now requires a persistable deterministic compile/replay recipe.

## Required next milestone

Close one cold-replayable executable inquiry cycle:

```text
OpenQuery / Ask
-> ProbeOperator
-> SurfacePlan
-> BackendRequest
-> durable request-before-dispatch
-> provider return
-> RawReturn
-> ActualEvent
-> decode/resolution
-> supported answer set
-> source continuation binding
-> runtime resumption
-> next Ask or Return
```

Preserve all current no-collapse laws.

Only then move toward active memory, real LLM provider integration, residual-driven method selection, fold/reopen learning, and self-revision.

## Final research use rule

Do not “implement the research.”

At each live repository residual:

```text
ask which final research distinction can break a bad implementation here;
turn only that distinction into the smallest executable fixture/contract;
implement the smallest existing-calculus composition that passes it;
leave the rest in research ancestry.
```

Continue autonomously from the new residual after each warranted ratchet.
