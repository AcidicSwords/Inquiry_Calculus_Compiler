# Phase B coarsest relation and refinement surface

This local Phase B pass reconstructs the four exact v2.0 definitions for the coarsest represented relation,
binding-level coexistence, reverse-inclusion relation refinement, and the typed relational
shape of nontrivial refinement discovery. It retains the adjacent “No vacuity from universal
relatedness” law as an unproved obligation. Formula, question, probe, protected-consequence, and
program semantics remain later layers; Gate B remains pending.

`coarsestRepresentedRelation` expresses universal joint admission only. It does not express
coexistence, causality, similarity, or discrimination. `ExistenceBoundary` makes coexistence
binding-supplied. `RepresentedRelation.refines` reverses inclusion, so a smaller extension is more
discriminating. `RefinementDiscoveryBoundary` retains only the typed proper-refinement shape; the
question and usefulness language is recorded as obligations rather than imported early.

```text
node tools/phase_b_refinement.js check
node tools/phase_b_refinement_check.js --compile
```

The independent checker regenerates five source identities and rejects twelve mutations: source
loss/promotion, Gate B promotion, carrier/boundary loss, universal or coexistence collapse, order
inversion, properness loss, obligation promotion, question leakage, and axiomatic completion. The
next residual is formula grammar.
