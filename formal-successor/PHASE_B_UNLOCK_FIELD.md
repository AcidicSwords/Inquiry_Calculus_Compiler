# Phase B unlock field

`FORMAL-B-UNLOCK-FIELD-001` binds the exact `FormalDefinition` record at v2.0 lines 4745–4779.
It represents observational, dynamic, and context-contract unlocks as distinct tagged active
triggers. An observational separator is an unlock, but not every unlock is observational.

The finite contrast activates each tag independently, rejects an inactive observation, and keeps
the observational and dynamic tags unequal. It does not evaluate factorization, continuation
descent, context invalidation, reopening, or license adequacy.

`node tools/unlock_field_check.js --compile` verifies the exact definition boundary, rejects five
targeted ablations, and audits five axiom-free proofs.
