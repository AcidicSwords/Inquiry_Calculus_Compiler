# Phase B local interrogative fixed point

## Source boundary

This ratchet reconstructs only the exact v2.0 definition at lines 5185–5191 and the adjacent
unproved reopening law at lines 5193–5197. It does not promote either statement into successor
semantics or establish Gate B.

## Factored meaning

The local fixed point is retained as a coverage-indexed property, not as another controller,
scheduler, history, or global completion predicate. Its executable boundary reuses the checked
root-frontier composition. A relevant root occurrence must have at least one supported exit:

1. determined by the retained profile;
2. factorably redundant;
3. inapplicable;
4. protected-continuation-equivalent with no required discharge;
5. explicitly blocked;
6. resource-bounded; or
7. dependent on a represented language, probe, or binding extension.

Closure additionally requires that no relevant root-frontier occurrence remains open after
redundancy is accounted for. Required discharge is protected independently: redundancy cannot
erase an executable required occurrence.

These exit predicates are a sum of previously represented relations. They are not seven lifecycle
states and do not select the next question. Blocked, resource-bounded, and extension-dependent
members remain explicit residuals rather than being discarded as completed work.

## Decisive contrast and contraction

The overcomplete finite contrast contains one witness for every allowed exit. It closes only under
explicit coverage and with no productive or required executable residual. Removing coverage,
adding one productive residual, or adding one required residual crosses the boundary. The retained
definition therefore needs only:

- explicit coverage;
- complete classification of relevant occurrences; and
- absence of a checked open root-frontier residual.

No separate closure machine survives primitive elimination.

## Positive reopening

A new separator, relation, probe, foil, binding, representation, protected horizon, or resource
regime is recorded as the typed kind of an enlargement. Reopening itself requires stronger
evidence: one exact occurrence was not open in the old context and is open in the enlarged
context. A changed label, an unsuccessful search, or an unmaterialized possibility cannot reopen
the field. The same context cannot reopen itself.

This is semantic field comparison only. It does not conflate semantic backward transport with
actual answer-dependent succession, and it grants no selection, support, warrant, execution,
Rust-semantic, or successor-promotion authority.

## Result

The v2.0 definition is reconstructed and the reopening claim is proved for the explicit positive
witness relation. The broader prose claim that some change *may* generate such a witness remains
conditional on actually establishing the newly open occurrence.
