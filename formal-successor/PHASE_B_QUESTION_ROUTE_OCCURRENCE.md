# Phase B — question-route occurrence and regeneration

`QuestionRouteOccurrence.lean` reconstructs the eight exact source records at v2.0 lines
5201–5239 as one bounded projection and obligation surface. It does not introduce another question
language, controller, semantic identity, or history.

The protected difference is that two routes may have the same semantic question and supported-answer
cardinality while differing in occurrence, continuation, resolution path, or support ancestry. The
finite breaker realizes precisely this collision and proves that all four identities remain
distinguishable. Supported-answer removal therefore exposes an occurrence-specific cue fiber;
question removal exposes checked occurrences rather than surface forms.

Pure/generated routes carry explicit absent actual coordinates. Exact regeneration is represented as
one protected class plus surviving typing, authority, event/path provenance, and reopening obligations.
The broad “no second question language or history” statement remains a named obligation until the
successor normalization supplies and checks the conservative-expansion relation. Frequency does not
warrant a method, and an answer-independent host policy cannot replace the stored continuation.

This is a predecessor-surface result only. It selects and executes nothing, grants no support or
warrant, changes no Rust semantics, and contributes no successor primitive.
