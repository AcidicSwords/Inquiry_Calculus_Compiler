# Phase B answer-conditioned unlock

`FORMAL-B-ANSWER-CONDITIONED-UNLOCK-001` binds the exact `FormalDefinition` and adjacent `Unproved`
`LegacyObligation` at v2.0 lines 5140–5159. Unlock is reconstructed as one exact readiness crossing:
the retained continuation is not ready before the checked Ask/whole-supported-Answer step and is
ready afterward under the same target use contract.

The finite model crosses that boundary once. An already-ready continuation, a stalled Ask, the
wrong contract, and the wrong continuation all fail. The successful unlock remains unresolved, so
readiness does not become an Answer, resolution, support, warrant, or standing.

Narrowing, discharge, defeat, reframe, reorientation, and extension are overlapping annotation
coordinates, not a runtime enum. Erasing every label preserves the transition identity. Defeat,
reframe, reorientation, and extension respectively require independent authority, accepted
representation/binding revision, retained canonical seed, and accepted successor evidence.

`node tools/answer_conditioned_unlock_check.js --compile` checks both exact source anchors, rejects
twenty-two structural ablations, and audits fourteen axiom-free proofs. It does not construct an
Answer, execute a step, resolve a continuation, dispatch a route, promote successor semantics, or
pass Gate B.
