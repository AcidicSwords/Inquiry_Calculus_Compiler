# Phase B approximate compression

`FORMAL-B-APPROXIMATE-COMPRESSION-001` binds fourteen exact `LegacyObligation` records at v2.0 lines 4713–4741,
retaining four `Unproved` and ten `Ambiguous` classifications. Approximation
keeps protected family, directional distortion, scalar error, error admission, resource gain,
residual, reopening, and continuation approximation as separate coordinates.

The finite contrast gives an over- and under-approximation the same scalar error. Only the former
satisfies the declared over direction; removing its residual coordinate also fails. Thus shared
scalar error cannot identify directional contracts, and approximate status is not exact protected
equivalence. No resource contract, semantic approximation decision, Rust change, promotion, or
Gate B pass is claimed.

`node tools/approximate_compression_check.js --compile` verifies the exact source/classification
boundary, rejects nine targeted source ablations, and audits four axiom-free proofs.
