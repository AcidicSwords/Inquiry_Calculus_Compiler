# Phase B continuation sufficiency

`FORMAL-B-CONTINUATION-SUFFICIENCY-001` binds one exact `LegacyObligation` record at v2.0 line
4664, retained as `Ambiguous`. A continuation and its protected scope are supplied. Descent means
an explicitly supplied target continuation commutes pointwise with the supplied quotient map.

The finite contrast proves descent for an identity continuation, then selects a protected source
continuation that splits a quotient fiber and proves that no descended target continuation exists.
It does not synthesize a continuation, establish relational descent beyond this function-shaped
model, prove universal factorization, regenerate a folded form, authorize executable reuse, or
advance Rust, successor promotion, or Gate B.

`node tools/continuation_sufficiency_check.js --compile` verifies the exact source/classification
boundary, rejects six targeted source ablations, and audits four axiom-free proofs.
