# Phase B question-pattern learning

Status: local checked predecessor-obligation realization; Formal Gate B remains pending.

## Exact source boundary

The source surface is the ten exact `LegacyObligation` records at lines 4348–4395: four display
records and six prose records covering the complete occurrence-indexed chain, operator and
state/boundary projections, protected aggregation licensing, applicability, exact recovery, and
occurrence-specific continuation selection.

## Decisive collision and contraction

Two finite realized chains share every operator-view and state/boundary-view coordinate while
differing in Ask reference, discharge member, checked continuation, and normalized next control.
Projection lookup therefore retains both chains. It cannot identify either one. The retained
selection boundary requires an explicit applicability proof, lookup membership, a recovered chain,
equality of that chain with the requested occurrence, and the continuation relation on the
recovered occurrence.

Protected-equivalence and applicability independently license aggregation; projection equality
does neither. Seven source ablations reject removal of occurrence identity, aggregation
applicability, protected-equivalence licensing, policy applicability, recovery, exact-chain
equality, or the selected-continuation relation. Thirteen proof contracts are axiom-free.

## Boundary

The result supplies pure lookup, aggregation, recovery, and selection relations. It does not
execute a question, reconstruct a chain automatically, treat content-equal raw returns as the same
event, define a universal scheduler or semantic truth relation, change Rust semantics, choose
successor primitives, or pass Gate B.
