# Phase B claim lifecycle

Status: local checked predecessor-obligation realization; Formal Gate B remains pending.

The source surface is the eight exact `LegacyObligation` records at v2.0 lines 4483–4512: six
remain `Ambiguous` and the candidate-standing and claim/fact laws remain `Unproved`. The checked
interface keeps generated `Claim`, semantic truth, partial `ReificationResult`, typed
`ReificationResidual`, exact-source-bound `CandidateFor`, `Candidate`, warrant, and standing
distinct. Standing has only a necessary
route through an independently supplied warrant relation; the model introduces no warrant system
or sufficient standing rule.

The decisive finite contrast uses a false generated Boolean claim that successfully reifies to a
candidate while both warrant and standing are absent. A separate true claim fails reification and
returns a binding inquiry residual, proving within the model that reification failure is not
semantic negation. Question syntax, answer type, model confidence, generation, and candidacy are
represented only as expressly unsupported promotion bases and cannot authorize standing.

Nine source ablations reject loss of claim-occurrence or statement provenance, partial reification,
typed failure, truth, generation, the warrant requirement, or the unsupported-promotion boundary. Eleven proof
contracts are axiom-free.

This local model does not establish a universal claim semantics, total reifier, truth decision,
warrant sufficiency, support environment, standing construction, answer selection, execution,
model-confidence authority, Rust semantics, successor primitives, or Gate B.
