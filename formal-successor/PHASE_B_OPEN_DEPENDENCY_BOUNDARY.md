# Phase B open dependency boundary

Status: local checked predecessor-boundary realization; Formal Gate B remains pending.

The exact source surface is the `FormalDefinition` at v2.0 lines 4537–4551 and the two adjacent
`Ambiguous` obligations at lines 4553–4560. An open dependency is positively required for an exact
candidate but is neither supplied by the exact support environment nor independently discharged.
The ambiguity in turning that dependency into `?d[...]` remains bounded by constructing only an
indexed ordinary question target. No Ask occurrence, Answer, event, support result, warrant, or
standing result is created.

The checked interface keeps requirement, environment supply, independent discharge, openness, and
question rendering separate. The question target is indexed by its context, exact environment, and
candidate and retains both the positive boundary proof and exact rendered question.

The decisive finite model has three required dependencies and one irrelevant dependency. One is supplied by the base environment,
one is not supplied but is independently discharged, and one is neither, so exactly the third is
open. Expanding the environment supplies and closes only that dependency. Expanding independent
discharge closes it without changing environment supply. This proves that not supplied is neither
required nor globally open, and that openness remains environment-relative.

Ten structural ablations are rejected and eleven proof contracts are axiom-free.

This local model does not execute the question, infer a negative answer, define closed support,
construct standing, or change Rust semantics. It selects no successor primitive and does not pass
Gate B.
