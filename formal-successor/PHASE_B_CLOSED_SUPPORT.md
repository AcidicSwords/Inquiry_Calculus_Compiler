# Phase B closed support

Status: local checked predecessor-obligation realization; Formal Gate B remains pending.

The exact source surface is eight `LegacyObligation` records at v2.0 lines 4564–4575. Seven remain
`Ambiguous`; the required-independent-check clause remains `Unproved`. The checked interface does
not promote those classifications. It gives the compound predecessor claim an explicit supplied
predicate so its conditions can be discriminated.

Closed support remains relative to an exact standing set, environment, and candidate. Its five
source clauses require: all environment premises requiring standing are in the supplied standing
set; applicability and scope both hold as distinct subrelations; the open dependency boundary is
empty; every required independent check succeeds; and no explicit inconsistency policy invalidates
the environment. Closure remains distinct from target standing.

The decisive finite model starts with every condition satisfied, then independently breaks standing
premise availability, applicability, scope, dependency-boundary emptiness, check success, and policy
non-invalidation. All six atomic ablations fail closure while the other coordinates remain fixed. A
route with an empty boundary but failed applicability remains unclosed. A no-policy route closes but
is distinguished from the explicitly accepted-policy route, preserving the source's universal
non-invalidation condition without inventing a policy-existence requirement.

Sixteen structural ablations are rejected and ten proof contracts are axiom-free.

This local model does not establish any source obligation globally, define grounded ingress or a
standing fixed point, execute checks, alter Rust semantics, select successor primitives, or pass
Gate B.
