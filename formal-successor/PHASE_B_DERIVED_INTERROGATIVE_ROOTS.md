# Phase B derived interrogative roots

`FORMAL-B-DERIVED-INTERROGATIVE-ROOTS-001` binds the sixteen exact `LegacyObligation`
records at v2.0 lines 5072–5101. The six roots are represented as transparent presentations
over ordinary relation and question material. `Orient` remains one root with forward and converse
orientations; it is not split into two primitives, and converse exposure does not synthesize an
inverse.

The checked model gives every root a canonical presentation, rejects a bare `Orient` and an
orientation attached to a non-orientation root, and reuses the same six roots after a checked
question occurrence is reified as ordinary relational material. Reification is therefore not a
seventh root. Every presentation separately denies new primitive status, actuality, authority,
and scheduling power.

This is source-bound primitive elimination, not successor semantics. It defines no question
solver, renderer, inverse, support, warrant, scheduler, program execution, or Rust behavior.
`node tools/derived_interrogative_roots_check.js --compile` checks all source anchors, rejects
seven structural ablations, and audits twelve axiom-free proofs.
