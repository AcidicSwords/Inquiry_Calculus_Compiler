# Phase A inventories

The live frontier will generate and independently check inventories for:

- every consequential v2.0 declaration and normative claim;
- every current Rust semantic module and public schema;
- every relevant registered conformance fixture.

Each row must carry a stable source identity, source locus, category, formalization destination or
explicit disposition, evidence status, and coverage/reopen metadata. This directory is not Gate A
evidence until the extraction grammar and omission breaker are executable.
