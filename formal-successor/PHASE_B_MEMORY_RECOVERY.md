# Phase B memory and recovery

Status: local checked predecessor-obligation realization; Formal Gate B remains pending.

## Exact source boundary

The source surface is the nineteen exact `LegacyObligation` records at lines 4399–4458. They
retain the denial of a separate memory-state kind, four recovery relations, the absence of
universal implications among them, nondominated reacquisition frontiers, a pinned baseline, and
the adjacent “keep experiences; compile routes” architecture law.

## Independence and frontier contrast

A four-coordinate finite model supplies a one-coordinate witness for every ordered pair of
different recovery relations. Thus use, reconstruction, evaluation, and reacquisition each can
hold while any selected other coordinate fails. A separate singleton-frontier model places the
with-material frontier strictly below the pinned baseline under a supplied comparison while direct
use and reconstruction remain false.

The comparison itself is required: replacing it with an always-false supplied relation removes
the advantage. Five source ablations reject collapse of any recovery coordinate or removal of the
frontier comparison. Five proof contracts are axiom-free.

## Boundary

The architectural law remains an explicit predecessor obligation. The result does not infer any
recovery coordinate from another, introduce a memory state kind, select an optimizer, totalize a
frontier, compress history, execute recovery, change Rust semantics, choose successor primitives,
or pass Gate B.
