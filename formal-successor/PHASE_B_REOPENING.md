# Phase B reopening

`FORMAL-B-REOPENING-001` binds nine exact `LegacyObligation` records at v2.0 lines 4792–4808,
retaining two `Unproved` and seven `Ambiguous` classifications. A reopening record requires an
active unlock, historical preservation, localized implication, restored recovery material, active
refinement, and recorded new breaker.

The finite record is lawful only when all components hold. Deleting historical ancestry and using
an inactive trigger both fail. This is not an implementation of factorization search, source
recovery, protected refinement, or semantic reopening completeness.

`node tools/reopening_check.js --compile` verifies the exact source boundary, rejects six
targeted ablations, and audits three axiom-free proofs.
