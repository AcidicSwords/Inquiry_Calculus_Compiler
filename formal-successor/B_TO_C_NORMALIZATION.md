# B→C successor normalization

Phase B now has a checked, source-bound predecessor surface through question-route occurrence. The
next operation is not another source-adjacent v2.0 reconstruction. It is normalization of the live
successor candidate by formal dependency.

The two supplied documents are recorded by digest in `NORMALIZATION_INPUTS.json`. They are
construction pressure, while the construction specification remains the sole successor procedure,
v2.0 remains the protected predecessor, and Lean returns remain checked candidate actuality.

## Candidate basis

The current smallest basis to test is:

1. typed carriers, contexts, and context-indexed families (`CtxFam`);
2. ordinary typed relations, identities, composition, and positive/negative relation use (`RegPred`);
3. finite or inductively generated typed relational structure (`Ind+`);
4. explicit partial binding as a presentation over that structure.

This is a candidate primitive boundary, not a promotion. Completion relations, question forms,
evaluation, observations, reciprocal mates, protected discrimination, determination, folds,
reopening, productivity, route views, named roots, methods, clocks, and backend policies must be
derived or must exhibit an exact missing structure and breaker.

## Accepted dependency corrections

- Semantic reach is the least closure under accepted seed, generation, and succession relations.
- A local closure certificate requires a post-fixpoint envelope and pointwise typed quiescence;
  an empty worklist is insufficient.
- A generator does not define the live field. Adequacy is protected role coverage plus explicit
  generator/representation-gap separation and required discharge.
- Fair service is execution or evidenced retirement. Generation fairness and selection fairness
  remain distinct.
- Convergence is conditional on a stable episode, progress-live opportunities, and a Noetherian
  closure-relevant order; universal termination is rejected.
- Finite carrier, finite generator, finite presentation, and finite closure certificate remain
  distinct.

These corrections are obligations in the normalization map until formalized. They do not mark a
gate passed.

## Question-driven execution

The controller reconstructs the field, chooses one represented live occurrence, and emits exactly
one `QuestionPacket`. The packet contains the exact question, canonical prose, answer type,
bound/open roles where represented, activation witness, protected consequence, minimal dependency
context, decisive breakers, and output contract. The backend never receives a second clock or the
whole construction procedure.

The local Qwen backend generates bounded candidates. The frontier model frames, reviews, invokes
independent checks, and accepts or rejects repository changes. Neither model warrants itself.
Attempt, token, time, and tool budgets bound an invocation; exhaustion creates `ResourceBounded`,
not semantic closure.

## Continuity

Every protected Phase-B capability remains referenced by the regenerative spine and its checked
artifact. Each P1–P20 derivation claim is either mapped to an existing planned theorem family,
recorded as an accepted dependency correction above, or retained as an unresolved typed Gate-C
obligation. O22 remains a later descriptor-interface frontier; it is not allowed to skip the
ambient basis and dependency proofs.

The live cursor therefore opens `FORMAL-C-AMBIENT-CAPABILITY-BASIS`: prove, break, or refine the
candidate basis and show which existing candidate primitives are derived from it before theorem
families depending on that basis are promoted.
