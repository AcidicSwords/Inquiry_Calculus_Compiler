# Phase B question-as-structured-hole boundary

This pass retains an open X-coordinate in its relation, fixed context, target region, and
completion fiber. It does not declare a filling complete or valid, nor supply question semantics.

```text
node tools/phase_b_question_structured_hole.js check
node tools/phase_b_question_structured_hole_check.js --compile
```
