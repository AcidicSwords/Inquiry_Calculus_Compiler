# Phase B executable question universe

`FORMAL-B-EXECUTABLE-QUESTION-UNIVERSE-001` binds nine exact `LegacyObligation` records at v2.0
lines 4860–4904, retaining one `Unproved` and eight `Ambiguous` classifications. An effectivity
profile retains generation, discharge, and tool budgets alongside decider, semidecider, and policy
coordinates. Executability requires semantic membership plus generation under that profile, so the
executable universe is a subset of the semantic universe.

The finite contrast keeps a hidden question semantically present in both profiles. It fails under a
zero-budget profile and succeeds under a profile with positive budget, a decider, and permission.
The same checked Ask occurrence is semantic in both profiles and executable only in the latter.

This does not define a generator, scheduler, resource semantics, general policy, or semantic
nonexistence result. `node tools/executable_question_universe_check.js --compile` verifies source
binding, rejects eight targeted ablations, and audits nine axiom-free proofs.
