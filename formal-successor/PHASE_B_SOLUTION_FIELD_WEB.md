# Phase B solution field of a web boundary

This pass represents a solution field as the indexed intersection of residual solution fibers
after other ports are treated as already bound. It does not establish refinement, answer meaning,
a solver, execution, or successor semantics.

```text
node tools/phase_b_solution_field_web.js check
node tools/phase_b_solution_field_web_check.js --compile
```
