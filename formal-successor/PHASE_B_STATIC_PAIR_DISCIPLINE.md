# Phase B static pair discipline

`FORMAL-B-STATIC-PAIR-DISCIPLINE-001` binds the two `Ambiguous` and one `Unproved`
`LegacyObligation` records at v2.0 lines 5118–5136. The finite reconstruction retains five
presentation identities: inverse, converse, logical breaker dual, binding-supplied adjoint, and
same-use reciprocal return. It is an obligation carrier and finite breaker surface, not successor
semantics or a proof of the universal source claim.

The decisive contrast uses a many-to-one relation. Its relational converse contains both source
points in one reverse fiber, while no function from the singleton target can be a strict inverse.
A supplied adjoint bridge remains separately lawful only when both the law and protected
continuation preservation are explicit; supplying that bridge does not collapse the presentation
kinds.

Same-use reciprocal return requires exact oriented-use identity, admission, positive departure,
declared coverage, the whole return fiber, and provenance. The complete witness passes and each
single-coordinate deletion fails. A bare generic backward presentation supplies none of this
actual answer-dependent succession material.

`node tools/static_pair_discipline_check.js --compile` checks the three exact source anchors,
rejects sixteen structural ablations, and audits sixteen axiom-free proofs. The universal pairwise
relation theorem, bridge construction, reciprocal execution, and every successor promotion remain
open.
