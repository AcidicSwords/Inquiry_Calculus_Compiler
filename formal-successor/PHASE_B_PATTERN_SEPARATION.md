# Phase B pattern separation and completion

`FORMAL-B-PATTERN-SEPARATION-001` binds four exact `LegacyObligation` records at v2.0 lines 4812–4820, retaining one `Unproved` and three `Ambiguous` classifications. A pattern boundary
keeps working similarity, representation, protected consequence, and protected difference
separate. Similar events with protected-different consequences require unequal representation
images; similarity alone does not license a merger.

Completion is a separate condition. It is licensed only when every pair of live completions is
protected-equivalent, or when an explicit approximate licence permits the ambiguity. The finite
contrast rejects a merged pair with different protected consequences and rejects a live historic /
novel completion pair with no approximate licence. It separately admits a split representation,
a singleton exact completion family, and an explicitly approximate completion family.

This realizes only the supplied finite boundary. It does not decide similarity, find separators,
establish global completion adequacy, identify semantic equivalence, change Rust, promote the
successor, or pass Gate B.
