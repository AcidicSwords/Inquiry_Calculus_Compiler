# Phase B traversal learning

Status: local checked predecessor-obligation realization; Formal Gate B remains pending.

## Exact source boundary

The source surface is the nine classified `LegacyObligation` records at lines 4310–4344: three
display records and six prose records covering path folding, protected probe nonredundancy,
independent later use, and the distinction between learning a relation and learning how to expose
it. Their exact identities and byte digests remain in
`formal-successor/PREDECESSOR_TEX_CLASSIFICATION.json`.

## One distinction, two routes

`PathFoldAdmissible` carries a checked `MethodPromotable` witness. Mere recurrence cannot create
that witness. `ProbeGenerated` instead carries an explicit state pair on which every represented
old probe agrees while the candidate probe and protected consequence both separate. It remains
inert. `ProbeAdmissible` additionally carries a later-use witness whose benefit relation is
independently supplied.

The overcomplete positive probe fixture crosses the nonredundancy boundary. Contraction retains
one old probe, one new separator, one protected separator, and one later-use witness. Removing old
basis agreement, new-probe separation, protected-consequence separation, or later-use evidence
rejects the corresponding generation or admission claim. Removing the method-promotion witness
rejects path folding.

## Boundary

These interfaces are pure. They do not execute paths or probes, infer old-basis agreement from
protected equality, turn novelty into nonredundancy, admit generated probes automatically, confer
standing or warrant, change Rust semantics, define successor primitives, or pass Gate B.
