# Phase B operational root aliases

`FORMAL-B-OPERATIONAL-ROOT-ALIASES-001` binds the exact `Unproved` `LegacyObligation` at v2.0
lines 5110–5114. `Contrast`, `Backchain`, `Preimage`, `Project`, `Ablate`, `Substitute`, `Localize`,
`Construct`, `Scrutinize`, `WhyNot`, and `HowCan` are retained as alias names with supplied
nonempty root-expansion data, not as primitives, runtime opcodes, methods, or scheduler phases.

The finite pressure model gives every alias a well-shaped sample root expansion. Some distinct
names deliberately share one root shape, so name count cannot establish primitive count. A
transparent expansion must separately preserve typing, applicability, whole supported-answer
behavior, authority, failure exits, provenance, and reopening while adding no primitive, runtime
opcode, or scheduling power. Ten one-coordinate depleted contrasts each fail transparency.

The exact alias definitions and their universal preservation proofs remain open; the sample
mapping is not successor semantics. `node tools/operational_root_aliases_check.js --compile`
checks the source anchor, rejects twelve structural ablations, and audits fourteen axiom-free
proofs.
