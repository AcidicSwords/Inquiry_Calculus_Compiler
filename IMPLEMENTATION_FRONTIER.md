# Implementation Frontier

This file is the sole live implementation cursor. Historical work belongs in Git, demonstrated behavior in `CONFORMANCE_STATUS.md`, and accepted choices or failures in the JSONL ledgers.

Repository actuality must be probed before acting. The current residual was derived from demonstrated implementation behavior through predecessor coordinate `594782c`; that coordinate is ancestry, not a claim that a tracked document can name its own final commit or worktree state.

## Immediate demonstrated predecessor

`QFRONTIER-REQDISCHARGE-001` established the finite required-safe frontier as required candidates union ordinary nondominated live candidates. A checked, unproductive required Check survives cheaper strict domination; a productive generated proposal remains optional; and required work can still dominate and remove an optional occurrence. The derived view retains exact occurrence, protected branch, resource, origin, source, and authority provenance without adding a scheduler, controller, canonical artifact, table, opcode, or history.

## Strongest live obligation

Establish finite local interrogative closure only within an explicit root, binding, effectivity, coverage, horizon, and resource boundary, preserving every non-closing exit.

<!-- LIVE_FRONTIER_BEGIN -->
id: QIFP-LOCAL-001
plan_phase: 9
goal: Derive a finite local interrogative fixed point over a caller-declared root/effectivity frontier, classifying every reachable occurrence by an admitted closing reason or an explicit residual exit.
protected_difference: Local closure means every relevant covered occurrence is determined, redundant, inapplicable, nonproductive without required discharge, or explicitly residualized; it never means that unrepresented extensions are absent, that incomplete coverage is negative, or that a required executable obligation may disappear.
discriminator: Close one exhaustively represented finite root whose reachable occurrences each carry a valid local closing reason; then remove one coverage edge and require Unknown, add one executable undischarged required occurrence and refuse closure, and preserve explicit blocked, resource-bounded, authority, and extension exits without treating them as success.
horizon: one finite declared root family, binding, effectivity relation, exact coverage certificate, protected horizon, resource bound, and no global traversal or scheduling claim
relevant_decisions: D-0024, D-0097, D-0098, D-0127, D-0135, D-0139, D-0143
relevant_failures: F-0003
if_pass: QIFP-REOPEN-001 require a positive admitted extension to reopen a previously closed local field
if_fail: reopen the smallest root/effectivity/coverage/classification/residual distinction; do not add a scheduler, controller, scalar closure score, authoritative table, or second history
<!-- LIVE_FRONTIER_END -->

## Smallest decisive fixture

Start from one finite declared root family and an exact effectivity graph whose reachable checked occurrences are all represented. Give every occurrence exactly one inspectable classification: determined, redundant/factorable, inapplicable, nonproductive with no required discharge, or an explicit blocked/resource/authority/extension residual. The exact field may close locally.

Remove one represented effectivity/coverage edge while leaving the visible occurrences unchanged; closure must become `Unknown`. Add one executable required occurrence without its discharge; closure must remain open rather than classifying it as nonproductive. Explicit non-success exits remain in the returned view and cannot be projected into task satisfaction.

The smallest wrong implementation closes from the visible candidate list alone, treats incomplete coverage as absence, or counts a blocked or undischarged required occurrence as successful closure.

## Prohibitions at this boundary

- Do not infer exact local closure from incomplete root/effectivity coverage.
- Do not collapse `Unknown`, blocked, resource-bounded, authority, or extension residuals into nonproductivity, resolution, or success.
- Do not close over an executable undischarged required occurrence.
- Do not synthesize a scheduler, controller, scalar closure score, global policy, authoritative table, or second history.

## Exit

On pass, update conformance and any warranted append-only decision, replace the live block with `QIFP-REOPEN-001`, and continue. On failure, preserve the actual return and reopen the earliest implicated root/effectivity/coverage/classification/residual relation rather than broadening the architecture.
