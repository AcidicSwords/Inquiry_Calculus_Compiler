# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-RELATION-SCHEMA-PORTS-001` regenerates three exact v2.0 source identities into a typed
schema/signature carrier with non-string named ports. The independent checker rejects ten source,
promotion, declaration, obligation, string, instance, semantic-question, and axiom mutations;
Gate B remains pending.

## Strongest live obligation

Elaborate v2.0 partial binding and completion fiber without treating the schema signature itself as
an instance, completion as a total function, or an open port set as a semantic question.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-PARTIAL-BINDING-FIBER
plan_phase: B
goal: Classify and reconstruct partial binding and completion-fiber syntax against typed schemas and ports, postponing relation satisfaction, total answer carriers, question semantics, refinement, probe, program, successor-primitive, and Rust meaning.
protected_difference: A partial binding records supplied typed coordinates outside an open set; a completion fiber is not a function, total carrier, or semantic question. Coverage and validity remain later obligations.
discriminator: Construct an overcomplete typed partial-binding/fiber carrier from exact sources, contrast it with total functions, untyped substitutions, concrete questions, and omission, then contract only after exact regeneration isolates the smallest responsible representation.
horizon: Partial binding and completion fiber only; schemas and named ports are fixed inputs, satisfaction, answer semantics, refinement, questions, probes, programs, standing, and all unrelated LegacyObligations remain explicit, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014, D-9015, D-9016, D-9017, D-9018, D-9019, D-9020
relevant_failures: F-9006, F-9007
if_pass: retain the typed partial-binding/fiber syntax and source obligations, then elaborate the next residual selected by the fiber boundary
if_fail: reopen the earliest binding/fiber source, typed coordinate, source-boundary, or semantic-question contamination relation; do not substitute total functions, untyped values, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct every exact partial-binding and completion-fiber alternative as an overcomplete typed
carrier, then contrast it with total functions, untyped substitutions, semantic questions, and
omission. Contract only after exact source regeneration and retained obligations distinguish the
smallest representable fiber surface.

## Prohibitions at this boundary

- Do not treat a partial binding as an untyped substitution or a fiber as a total function.
- Do not treat an open port set or fiber as a semantic question or theorem authority.
- Do not use axioms, proof gaps, or Rust behavior to bypass schema typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest source-regenerative partial-binding/fiber carrier, explicit source
obligations, and independent breakers, then select the next residual. On failure, preserve the
return and reopen the earliest binding, fiber, source, or semantic-question boundary relation.
