# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-STATE-WORKING-PRESENTATION-001` machine-checks accepted presentation, working presentation,
active-view derivation, and reconstructible-state authority limits as source obligations. It keeps folding,
presentation, compression, state mutation, and successor meanings open; Gate B remains pending.

## Strongest live obligation

Elaborate the operator occurrence index while preserving the compact state/operator/return/successor-state/boundary tuple, operator-indexed event reference lookup, and non-duplicated snapshot constraint without building an index, looking up history, or copying snapshots.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-OPERATOR-OCCURRENCE-INDEX
plan_phase: B
goal: Determine the smallest source-regenerative boundary for the v2.0 operator occurrence index, preserving compact occurrence coordinates and operator-indexed event references as a lookup over existing history without snapshot duplication.
protected_difference: Operator-index constraints are predecessor obligations; they are not an index builder, history query engine, state snapshot copy, occurrence evaluator, or semantic authority decision procedure.
discriminator: Contrast the source-bound compact tuple and lookup relation with a foil that duplicates snapshots, constructs a new history, executes lookup, or omits a state/operator/return/boundary coordinate; retain only the exact source-supported index boundary after regeneration and independent checking.
horizon: Operator-occurrence-index boundary only; state-working-presentation is fixed input, index construction, history lookup, snapshot duplication, occurrence evaluation, semantic authority, and all unrelated material remain explicit unless independently formalized and checked, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014, D-9015, D-9016, D-9017, D-9018, D-9019, D-9020, D-9021
relevant_failures: F-9006, F-9007
if_pass: retain the smallest checked state-working-presentation boundary and then elaborate the operator occurrence index
if_fail: reopen the earliest accepted fold, event history, presentation, active view, compression, hidden memory, mutation, obligation, or source boundary relation; do not substitute proofs, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct a snapshot-duplicating, new-history, executed-lookup, or coordinate-dropping index foil, then contrast it
with the declared compact tuple and existing-history lookup. Contract only after exact source regeneration and retained
obligations distinguish the smallest operator-index boundary.

## Prohibitions at this boundary

- Do not build an index, query history, evaluate occurrences, or duplicate snapshots in this carrier.
- Do not treat a lookup projection as a competing history or an authority-bearing state copy.
- Do not use axioms, proof gaps, or Rust behavior to bypass schema typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest operator-occurrence-index boundary, explicit source obligations, and independent breakers,
then select the next residual. On failure, preserve the return and reopen the earliest state, operator, return, boundary,
event reference, lookup, snapshot, history, or source boundary relation.
