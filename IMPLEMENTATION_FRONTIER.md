# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-REPRESENTATION-QUESTION-001` machine-checks candidate distinction/relevance and distinct
scoped-factorization/protected-sufficiency conditions. It keeps candidate selection, proof,
answer selection, solver, execution, and successor meanings out of the boundary; Gate B remains pending.

## Strongest live obligation

Elaborate grain question without treating a grain-difference/relevance predicate as a successful,
coarsest, answer-selecting, executable, or successor-semantic representation.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-GRAIN-QUESTION
plan_phase: B
goal: Determine the smallest source-regenerative boundary for v2.0 grain questions, preserving a grain-indexed representation, pair distinction, protected consequence difference, separating grain, coarsest representation, answer semantics, successor primitives, and Rust meaning as open unless independently established.
protected_difference: A grain question predicates a grain-indexed representation difference and protected consequence exposure; it is distinct from a successful or coarsest grain, answer, solver, program, or successor semantics.
discriminator: Contrast a source-bound grain-difference/protected-exposure question form with successful separator, coarsest representation, selected-answer, solver, program, semantic-evaluation, and omission claims; retain only the exact source-supported grain boundary after regeneration and independent checking.
horizon: Grain-question boundary only; representation-question syntax is fixed input, grain relevance, successful separation, coarsestness, answer semantics, execution, and all unrelated LegacyObligations remain explicit unless their exact source classification and independent checks establish otherwise, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014, D-9015, D-9016, D-9017, D-9018, D-9019, D-9020, D-9021
relevant_failures: F-9006, F-9007
if_pass: retain the smallest checked grain-question boundary and then elaborate the next residual selected by its unresolved relation
if_fail: reopen the earliest grain, representation, pair, consequence, separation, coarsestness, answer, program, or source boundary relation; do not substitute proofs, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct a decisive source-bound grain-predicate foil, then contrast it with successful separator,
coarsest representation, selected-answer, solver, program, semantic-evaluation, and omission
claims. Contract only after exact source regeneration and retained obligations distinguish the
smallest grain-question boundary.

## Prohibitions at this boundary

- Do not treat a grain question as a successful/coarsest representation, selected answer, total solver, program, or successor semantics.
- Do not use a source question form alone as grain separation, coarsestness, answer-validity, or solver evidence.
- Do not use axioms, proof gaps, or Rust behavior to bypass schema typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest source-regenerative grain-question boundary, explicit source
obligations, and independent breakers, then select the next residual. On failure, preserve the
return and reopen the earliest grain, representation, pair, consequence, separation, coarsestness,
answer, program, or source boundary relation.
