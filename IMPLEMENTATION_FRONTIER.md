# Implementation Frontier

This file is the sole live implementation cursor. Historical work belongs in Git, demonstrated behavior in `CONFORMANCE_STATUS.md`, and accepted choices or failures in the JSONL ledgers.

Repository actuality must be probed before acting. The current residual was derived from demonstrated implementation behavior through predecessor coordinate `3829944`; that coordinate is ancestry, not a claim that a tracked document can name its own final commit or worktree state.

## Immediate demonstrated predecessor

`QLOWER-001` established conservative erasure for the three controlled backward contracts. Finite implementation-only labels lower through freshly checked `OpenQuery`, `SourceConfig`, `IProg`, and occurrence data only when every protected query, answer-carrier, mode, authority, branch, continuation, coverage, use, binding, renderer, and provenance field equals a separately checked unannotated predecessor. Label renaming is inert; query, mode, continuation, branch, coverage, and use substitutions reject. This adds no semantic root, macro language, canonical annotation artifact, table, opcode, or second semantics.

## Strongest live obligation

Preserve protected-different realized `Ask` occurrences and port-indexed evidence chains through cold reconstruction even when semantic questions, operators, raw returns, candidate projections, or one explicitly checked event are equal.

<!-- LIVE_FRONTIER_BEGIN -->
id: QACTUAL-SEPARATION-001
plan_phase: 6,7,8
goal: Cold-reconstruct two protected-different realized Ask occurrences and one mixed-port evidence bundle without collapsing occurrence or port-indexed provenance.
protected_difference: Equal semantic question, operator, raw-return bytes, candidate projection, or shared checked event does not imply one realized Ask occurrence, one continuation, or one evidence route. Position, slot, environments, binding/compiler versions, provenance, request/operator/raw/history membership, route, resolution path, event, and port index remain independently checkable coordinates.
discriminator: Build a file-backed paired-occurrence fixture whose two checked source configurations share question/operator/raw/member projections but have protected-different continuations; reject each forged occurrence field, execute and persist ordinary events, close and reopen the store, and cold-reconstruct each exact successor with zero redispatch while rejecting every swapped request/operator/raw/event/ledger/provenance link. Add a mixed-port bundle with distinct port-indexed route/path/event chains and a case where two ports intentionally name one separately checked event without collapsing their route evidence.
horizon: two finite linear Ask/Return source configurations, one exact finite decoded supported answer per occurrence, one file-backed store reopen, one finite multi-port bundle, and one explicitly shared checked event; no general causal graph, scheduler, or multi-event completeness theorem
relevant_decisions: D-0048, D-0108, D-0109, D-0110, D-0114, D-0120, D-0128, D-0131, D-0149
relevant_failures: F-0001, F-0003
if_pass: QLIFT-ALLPATHS-001 preserve every tagged path and child authority through non-singleton dependent continuation
if_fail: reopen the earliest occurrence/request/event/route/path/port-index/replay identity relation; do not collapse to endpoint identity or add a second history, event table, scheduler, controller, or opcode
<!-- LIVE_FRONTIER_END -->

## Smallest decisive fixture

Start from two ordinary checked `Ask` occurrences with equal semantic question, provider/operator, exact raw bytes, and decoded candidate projection but different source positions and protected continuations. Seal both occurrence identities before dispatch. Persist only accepted artifacts and ordinary events, then reconstruct each event-linked decoded answer, exact support route, occurrence, and successor after a real store close/reopen without reusing pre-close runtime values or invoking the provider again.

Construct a separate finite mixed-port view over one checked question occurrence. Give each port an explicit route and resolution path, first with distinct events and then with two ports deliberately sharing one event whose membership is independently checked. The shared event may be referenced twice; the port-indexed evidence coordinates may not merge.

The smallest wrong implementation keys realized work by semantic question, operator/raw tuple, candidate member, or event alone, causing one continuation or one port route to overwrite the other. Mutate every protected coordinate separately and require a named rejection rather than a generic endpoint mismatch.

## Prohibitions at this boundary

- Do not infer occurrence identity from semantic question, operator, raw return, candidate member, or final endpoint.
- Do not infer causal order from ledger order or merge port evidence merely because an event is shared.
- Do not redispatch during cold reconstruction or reuse pre-close runtime, suspension, bound-answer, or catalog objects.
- Do not add a second event/history authority, occurrence table, port-evidence table, scheduler, controller, or opcode before a reconstruction breaker proves ordinary artifacts and events insufficient.

## Exit

On pass, update conformance and any warranted append-only decision, replace the live block with `QLIFT-ALLPATHS-001`, and continue. On failure, preserve the actual return and reopen the earliest implicated occurrence/request/event/route/path/port-index/replay relation rather than broadening the architecture.
