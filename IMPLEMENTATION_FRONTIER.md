# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-RESIDUAL-QUESTION-GENERATOR-001` machine-checks whole-answer, checked-Ask,
retained-route, residual, and discriminative-or-required next-question boundaries. It keeps
supported-answer semantics, productivity, occurrence actuality, solver, and successor meanings out
of the boundary; Gate B remains pending.

## Strongest live obligation

Elaborate the checked Ask occurrence while preserving exact occurrence identity, source syntax, explicit environment, answer slot, continuation, and provenance.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-CHECKED-ASK-OCCURRENCE
plan_phase: B
goal: Determine the smallest source-regenerative boundary for v2.0 checked Ask occurrence, preserving immutable occurrence identity, source syntax, explicit environment, answer slot, continuation package, versions, and provenance.
protected_difference: A checked Ask occurrence is an inspectable source-program occurrence with exact identity and context, not a raw return, event, selected answer, host closure, or hidden model policy.
discriminator: Contrast source-bound checked Ask occurrence with detached syntax, raw-return/event/answer conflation, host closure, hidden policy, answer selection, solver, program, and semantic-evaluation claims; retain only the exact source-supported occurrence boundary after regeneration and independent checking.
horizon: Checked-Ask-occurrence boundary only; residual question generator is fixed input, source program typing, event actuality, raw return, resolution, answer semantics, execution, and all unrelated LegacyObligations remain explicit unless their exact source classification and independent checks establish otherwise, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014, D-9015, D-9016, D-9017, D-9018, D-9019, D-9020, D-9021
relevant_failures: F-9006, F-9007
if_pass: retain the smallest checked Ask-occurrence boundary and then elaborate the next residual selected by its unresolved relation
if_fail: reopen the earliest occurrence, syntax, environment, slot, continuation, provenance, or source boundary relation; do not substitute proofs, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct a decisive source-bound raw-return/selected-answer and hidden-policy foil, then contrast it with immutable
occurrence identity, source Ask syntax, explicit environment, answer slot, continuation package, version and
provenance, solver, and omission claims. Contract only after exact source regeneration and retained obligations
distinguish the smallest checked-Ask-occurrence boundary.

## Prohibitions at this boundary

- Do not conflate a checked Ask occurrence with a raw return, event, selected answer, host closure, or hidden policy.
- Do not treat this carrier as answer selection, a solver, execution, or successor semantics.
- Do not use axioms, proof gaps, or Rust behavior to bypass schema typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest checked-Ask-occurrence boundary, explicit source obligations, and independent breakers,
then select the next residual. On failure, preserve the return and reopen the earliest occurrence, syntax, environment,
slot, continuation, provenance, or source boundary relation.
