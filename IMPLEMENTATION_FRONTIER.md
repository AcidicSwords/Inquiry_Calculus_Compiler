# Implementation Frontier

This file is the sole live implementation cursor. Historical work belongs in Git, demonstrated behavior in `CONFORMANCE_STATUS.md`, and accepted choices or failures in the JSONL ledgers.

Repository actuality must be probed before acting. The current residual was derived from demonstrated implementation behavior through predecessor coordinate `e2ba388`; that coordinate is ancestry, not a claim that a tracked document can name its own final commit or worktree state.

## Immediate demonstrated predecessor

`QROUTE-REGEN-001` established that an omitted route position regenerates only when every member of an exact finite residual completion fiber reconstructs and shares the full protected route signature. A two-member constant fiber regenerates one route; an equal-Return-endpoint fiber split by source occurrence provenance returns a positive separator. This adds no route history, cache authority, learned method, scheduler, controller, canonical artifact, table, or opcode.

## Strongest live obligation

Authorize actual route-node ablation only when typing, authority, provenance, protected behavior, exact regeneration, and reopening remain demonstrably invariant after removal.

<!-- LIVE_FRONTIER_BEGIN -->
id: QROUTE-ABLATE-001
plan_phase: 16
goal: Apply one finite route-node removal only through the demonstrated exact regeneration witness, then compare the realized reduced route against every protected obligation.
protected_difference: Regenerability is necessary but actual ablation is licensed only when the reduced representation still reconstructs typing, authority, occurrence/answer/event/support provenance, protected successor behavior, and reopening. A successful endpoint or cached reconstruction alone cannot authorize removal.
discriminator: Build one route representation with a removable transparent node and one protected consumer for each required signature coordinate. Derive its exact regeneration witness, ablate the node, reconstruct through the retained basis, and compare all protected consumers. Reject separate foils that lose authority provenance and reopening while preserving the visible endpoint.
horizon: one finite route representation, one candidate transparent node, one exact regeneration result, finite protected consumers, no global route minimality or learned method promotion
relevant_decisions: D-0024, D-0083, D-0097, D-0127, D-0135, D-0139, D-0145, D-0146
relevant_failures: F-0003
if_pass: QRENDER-001 preserve existential preimage, universal guarantee, and same-use reciprocal distinctions through controlled rendering
if_fail: reopen the smallest node-transparency/regeneration/consumer/authority/provenance/reopening distinction; do not add route history, cache authority, learned method, scheduler, controller, table, or opcode
<!-- LIVE_FRONTIER_END -->

## Smallest decisive fixture

Represent one transparent finite route node as derived data between a retained basis and a protected route consumer. Before removal, derive the exact whole-fiber regeneration result from `QROUTE-REGEN-001` and seal all protected signature coordinates.

Remove only that derived node, reconstruct the route from the retained basis, and compare exact binding, occurrence, whole answer, event/operator/raw return, observation/support routes, continuation, successor, authority evidence, and reopening coordinate. Construct two foils with the same visible endpoint: one erases authority/support provenance and one changes reopening identity. Both must reject ablation.

The smallest wrong implementation treats regeneration as self-authorizing, tests only the endpoint, or permits the removed node/cache to provide the evidence for its own removal.

## Prohibitions at this boundary

- Do not let the node or cache warrant its own removal.
- Do not test only the visible endpoint; preserve every protected signature coordinate and reopening route.
- Do not mutate canonical source programs, accepted artifacts, ordinary event history, or standing meaning at this derived boundary.
- Do not add route history, cache authority, learned method, scheduler, controller, table, opcode, or second history.

## Exit

On pass, update conformance and any warranted append-only decision, replace the live block with `QRENDER-001`, and continue. On failure, preserve the actual return and reopen the earliest implicated node-transparency/regeneration/consumer/authority/provenance/reopening relation rather than broadening the architecture.
