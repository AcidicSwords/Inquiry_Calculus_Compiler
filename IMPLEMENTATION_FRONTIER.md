# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-SEQUENCING-COMPOSITION-001` machine-checks bind and equation shapes plus distinct
protected-equivalence identity shapes without a bind evaluator or proof. It keeps equation validity,
equivalence, execution, solver, and successor meanings open; Gate B remains pending.

## Strongest live obligation

Elaborate operational semantics while preserving runtime configuration, Return/Branch/Probe transition shapes, event-spine ordering, and raw-return immutability without execution or transition-proof promotion.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-OPERATIONAL-SEMANTICS
plan_phase: B
goal: Determine the smallest source-regenerative boundary for v2.0 operational semantics, preserving runtime configuration, Return/Branch/Probe transition shapes, ordinary event-spine ordering, and raw-return immutability.
protected_difference: Runtime transition prose and displays are predecessor obligations; they are not yet an executable evaluator, actual event mechanism, or proved transition system.
discriminator: Contrast source-bound transition carriers with an executable runtime, event-order shortcut, raw-record mutation, or automatic transition proof; retain only the exact source-supported operational boundary after regeneration and independent checking.
horizon: Operational-semantics boundary only; sequencing and composition is fixed input, transition validity, runtime state, event actuality, raw-return semantics, execution, answer semantics, and all unrelated LegacyObligations remain explicit unless independently formalized and checked, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014, D-9015, D-9016, D-9017, D-9018, D-9019, D-9020, D-9021
relevant_failures: F-9006, F-9007
if_pass: retain the smallest checked sequencing-and-composition boundary and then elaborate operational semantics
if_fail: reopen the earliest bind, equation, protected-equivalence, obligation, or source boundary relation; do not substitute proofs, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct an executable-runtime or mutable-raw-record foil, then contrast it with runtime configuration, transition
shapes, ordinary event-spine ordering, and raw-return immutability. Contract only after exact source regeneration and
retained obligations distinguish the smallest operational-semantics boundary.

## Prohibitions at this boundary

- Do not turn transition shapes into an executable evaluator or automatic transition proof.
- Do not replace ordinary event-spine ordering with event order alone or permit raw-record mutation.
- Do not use axioms, proof gaps, or Rust behavior to bypass schema typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest operational-semantics boundary, explicit source obligations, and independent breakers,
then select the next residual. On failure, preserve the return and reopen the earliest configuration, transition,
event-spine, raw-record, execution, or source boundary relation.
