# Implementation Frontier

This file is the sole live implementation cursor. Historical work belongs in Git, demonstrated behavior in `CONFORMANCE_STATUS.md`, and accepted choices or failures in the JSONL ledgers.

Repository actuality must be probed before acting. The current residual was derived from demonstrated implementation behavior through predecessor coordinate `361109d`; that coordinate is ancestry, not a claim that a tracked document can name its own final commit or worktree state.

## Immediate demonstrated predecessor

`QRESOLUTION-GATE-001` established the finite derived five-way resolution boundary without a second resolver authority. A composed finite relation preserves all compatible outputs in canonical order; exact empty, typed undefined, decoded-but-unsupported, partial-coverage, and whole proof-carrying supported results retain distinct payloads. Only the supported result, an all-Probe discharge bundle, and an exact source re-walk construct a resolved occurrence; an equal-question source program belonging to another occurrence rejects. This adds no canonical outcome artifact, general relation engine, search-completeness claim, scheduler, controller, table, opcode, or persistence surface.

## Strongest live obligation

Preserve source/runtime quotation result types and allow interpretation only under the exact admitted binding and compiler-version coordinates, without executing, actualizing, supporting, or warranting quoted code.

<!-- LIVE_FRONTIER_BEGIN -->
id: QCODE-TYPING-001
plan_phase: 3,5
goal: Derive typed nonexecuting source and runtime quotations whose interpretation is partial and exactly indexed by result type, binding version, and compiler version.
protected_difference: Two quotations can share code-shaped payload or endpoint while denoting different result types, bindings, or compiler versions. Treating a quotation as executable, accepting a version/binding mismatch, or returning a value from quotation alone would change semantic type, actuality, authority, or control without an admitted interpretation route.
discriminator: Construct one source and one runtime quotation for the same result type under two compiler versions. Recheck canonical quotation identity and type on cold-decoded values; admit interpretation only for the exact binding/compiler pair and reject result-type, binding, and compiler-version substitutions. Prove quotation and failed interpretation create no event, raw return, supported answer, standing result, warrant, or runtime execution.
horizon: one finite source quotation, one finite runtime quotation, one result type, two compiler versions, two binding versions, exact successful interpretation, and three mismatch refusals; no code evaluation, dynamic loader, general compiler, provider invocation, optimization, cross-binding bridge, or quotation persistence policy
relevant_decisions: D-0125, D-0127, D-0152
relevant_failures: F-0001, F-0002
if_pass: rederive the single strongest residual from the stable phase plan and pending conformance after quotation typing; QREADY-UNLOCK-001 and QREADY-NONUNLOCK-001 already pass and must not be reopened without a new breaker
if_fail: reopen the earliest quotation identity, result-type, binding, compiler-version, interpretation, or execution-separation relation; do not introduce dynamic code execution, an evaluator authority, scheduler, controller, table, or opcode
<!-- LIVE_FRONTIER_END -->

## Smallest decisive fixture

Construct one first-order source quotation and one runtime quotation with the same explicit result type, then change only compiler version, binding version, and result type. Each quotation must decode/recheck to its exact identity without execution. The successful interpretation must name the exact quotation, result type, binding, and compiler version; each changed coordinate must return a typed undefined/mismatch result rather than an untyped exception, substituted program, or result value.

The smallest wrong implementation treats `Code(A)` as `Prog(A)`, checks code identity but not the quoted result type, accepts either version because its code payload is equal, or performs evaluation while checking quotation.

## Prohibitions at this boundary

- Do not execute, actualize, decode a provider return, support, stand, or warrant a quotation merely because it is well typed or structurally valid.
- Do not erase source/runtime kind, result type, binding version, or compiler version from quotation or interpretation identity.
- Do not treat a failed interpretation as negative semantic evidence, exact emptiness, a value, or an alternative compiler selection.
- Do not add a dynamic evaluator, code-loading authority, scheduler, controller, table, opcode, or persistence schema before a finite typed quotation breaker proves ordinary immutable artifacts insufficient.

## Exit

On pass, update conformance and any warranted append-only decision, replace the live block with `QQUESTION-READINESS-001`, and continue. On failure, preserve the actual return and reopen the earliest implicated quotation/type/binding/compiler/interpretation/execution-separation relation rather than broadening the architecture.
