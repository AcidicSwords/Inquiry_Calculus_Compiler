# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-AUTHORITATIVE-ANCESTRY-PROJECTIONS-001` machine-checks one authoritative ancestry plus event/revision
projection shapes and the non-rewrite limit as source carriers. It keeps ancestry construction, append/acceptance,
edge evaluation, and successor meanings open; Gate B remains pending.

## Strongest live obligation

Elaborate the event record while preserving every record coordinate, source-Ask option shape, direct/legacy versus source-Ask distinction, and raw-return/provenance requirements without creating, linking, decoding, or accepting events.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-EVENT-RECORD
plan_phase: B
goal: Determine the smallest source-regenerative boundary for the v2.0 canonical actual event record, preserving state, source occurrence, question/schema, boundary, operator, raw return, successor state, grain, route, and provenance/replay coordinates along with source-Ask option shape.
protected_difference: Event-record constraints are predecessor source carriers and obligations; they are not an event constructor, source-link verifier, event decoder, raw-return interpreter, route executor, replay engine, acceptance decision, or semantic authority decision procedure.
discriminator: Contrast the complete source-bound event record with a foil that omits a coordinate, treats direct/legacy and source-Ask events identically, accepts matching endpoint data without exact linkage, decodes raw return as actuality, or replaces the ordinary event spine with a reciprocal view; retain only the exact source-supported event boundary after regeneration and independent checking.
horizon: Event-record boundary only; authoritative ancestry is fixed input, event construction/linkage/decoding/acceptance, raw-return semantics, route/replay execution, reciprocal actuality, semantic authority, and all unrelated material remain explicit unless independently formalized and checked, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014, D-9015, D-9016, D-9017, D-9018, D-9019, D-9020, D-9021
relevant_failures: F-9006, F-9007
if_pass: retain the smallest checked authoritative-ancestry-and-projections boundary and then elaborate the event record
if_fail: reopen the earliest root, event, revision, projection, reference, append, rewrite, edge, obligation, or source boundary relation; do not substitute proofs, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct a coordinate-dropping, source-link-collapsing, raw-return-decoding, or reciprocal-view-as-history foil, then
contrast it with the declared event record and ordinary event spine. Contract only after exact source regeneration and
retained obligations distinguish the smallest event-record boundary.

## Prohibitions at this boundary

- Do not construct, link, decode, replay, accept, or rewrite events in this carrier.
- Do not replace the ordinary event spine with derived reciprocal views or treat raw return as decoded actuality.
- Do not use axioms, proof gaps, or Rust behavior to bypass schema typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest event-record boundary, explicit source obligations, and independent breakers, then select
the next residual. On failure, preserve the return and reopen the earliest state, source occurrence, question, boundary,
operator, return, route, provenance, linkage, decode, or source boundary relation.
