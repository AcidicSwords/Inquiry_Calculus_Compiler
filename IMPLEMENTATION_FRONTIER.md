# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-GRAIN-QUESTION-001` machine-checks grain-indexed representation distinction and declared
protected exposure. It keeps successful separation, coarsestness, answer selection, solver,
execution, and successor meanings out of the boundary; Gate B remains pending.

## Strongest live obligation

Elaborate probe/tool invention question without treating a candidate that distinguishes an old
probe-equivalence cell as an implemented, successful, answer-selecting, executable, or successor-semantic tool.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-PROBE-TOOL-INVENTION-QUESTION
plan_phase: B
goal: Determine the smallest source-regenerative boundary for v2.0 probe/tool invention questions, preserving a current probe basis, old-probe equality, candidate distinction, horizon non-equivalence, implementation, return, answer semantics, successor primitives, and Rust meaning as open unless independently established.
protected_difference: A probe/tool invention question predicates a candidate distinction within a current probe-equivalence cell that is horizon-nonequivalent; it is distinct from an implemented or successful tool, answer, solver, program, or successor semantics.
discriminator: Contrast a source-bound old-basis-equality/candidate-distinction/non-equivalence question form with implemented tool, actual return, successful discrimination, selected-answer, solver, program, semantic-evaluation, and omission claims; retain only the exact source-supported probe-invention boundary after regeneration and independent checking.
horizon: Probe/tool-invention-question boundary only; grain-question syntax is fixed input, basis membership, tool implementation, return, success, answer semantics, execution, and all unrelated LegacyObligations remain explicit unless their exact source classification and independent checks establish otherwise, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014, D-9015, D-9016, D-9017, D-9018, D-9019, D-9020, D-9021
relevant_failures: F-9006, F-9007
if_pass: retain the smallest checked probe/tool-invention-question boundary and then elaborate the next residual selected by its unresolved relation
if_fail: reopen the earliest probe basis, pair, candidate, implementation, return, success, answer, program, or source boundary relation; do not substitute proofs, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct a decisive source-bound probe-invention predicate foil, then contrast it with implemented
tool, actual return, successful discrimination, selected-answer, solver, program, semantic-evaluation,
and omission claims. Contract only after exact source regeneration and retained obligations distinguish
the smallest probe-invention-question boundary.

## Prohibitions at this boundary

- Do not treat a probe/tool invention question as an implemented/successful tool, selected answer, total solver, program, or successor semantics.
- Do not use a source question form alone as tool implementation, actual return, answer-validity, or solver evidence.
- Do not use axioms, proof gaps, or Rust behavior to bypass schema typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest source-regenerative probe/tool-invention-question boundary, explicit
source obligations, and independent breakers, then select the next residual. On failure, preserve
the return and reopen the earliest probe basis, pair, candidate, implementation, return, success,
answer, program, or source boundary relation.
