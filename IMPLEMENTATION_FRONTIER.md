# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-QUESTION-CONDITIONED-LLM-FIELD-001` machine-checks the question/time-indexed six-projection field and
the no-separate-memory-ontology constraint as source obligations. It keeps construction, relevance, retrieval,
dispatch, evaluation, and successor meanings open; Gate B remains pending.

## Strongest live obligation

Elaborate the three distinct orders while preserving ledger order, binding-defined realized succession, and inquiry traversal as separate typed relations without defining an equation among them.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-THREE-DISTINCT-ORDERS
plan_phase: B
goal: Determine the smallest source-regenerative boundary for the v2.0 three-order distinction, preserving authoritative event-record storage order, binding-defined realized/domain succession, and distinction-boundary inquiry traversal as separately typed orders.
protected_difference: The three orders are predecessor obligations; they are not a common order, an equation, a conversion, a synchronization mechanism, or a semantic authority decision procedure.
discriminator: Contrast the declared three order identities with a foil that equates, derives, converts, or synchronizes ledger, realized, and traversal order; retain only the exact source-supported non-collapse boundary after regeneration and independent checking.
horizon: Three-distinct-orders boundary only; question-conditioned LLM field is fixed input, order semantics, conversion, causality, synchronization, semantic authority, and all unrelated LegacyObligations remain explicit unless independently formalized and checked, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014, D-9015, D-9016, D-9017, D-9018, D-9019, D-9020, D-9021
relevant_failures: F-9006, F-9007
if_pass: retain the smallest checked question-conditioned-LLM-field boundary and then elaborate the three distinct orders
if_fail: reopen the earliest question/time, projection, ontology, construction, relevance, retrieval, method, reopening, obligation, or source boundary relation; do not substitute proofs, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct an all-orders-equal or automatic-order-conversion foil, then contrast it with the declared ledger, realized,
and traversal order identities. Contract only after exact source regeneration and retained obligations distinguish the
smallest three-orders boundary.

## Prohibitions at this boundary

- Do not define, equate, convert, or synchronize the three orders in this carrier.
- Do not treat ledger order as realized succession or inquiry traversal, or any other pair as interchangeable.
- Do not use axioms, proof gaps, or Rust behavior to bypass schema typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest three-distinct-orders boundary, explicit source obligations, and independent breakers,
then select the next residual. On failure, preserve the return and reopen the earliest order identity, equation,
conversion, synchronization, causality, traversal, or source boundary relation.
