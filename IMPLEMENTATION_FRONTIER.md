# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-OPERATOR-OCCURRENCE-INDEX-001` machine-checks the compact operator occurrence tuple, existing-history
lookup, and no-snapshot-duplication constraint as source obligations. It keeps index construction, lookup,
evaluation, snapshot copying, and successor meanings open; Gate B remains pending.

## Strongest live obligation

Elaborate recurrent probes while preserving contract coordinates, comparable-occurrence relation, standing bridge, and non-identity of wording/operator identifier without deciding contracts, bridges, or comparability.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-RECURRENT-PROBES
plan_phase: B
goal: Determine the smallest source-regenerative boundary for v2.0 recurrent probes, preserving relation role, binding, grain, applicability, comparator, protected continuation family, decoder version, same-contract/bridge comparability, and the required non-collapse of wording/operator identifier into probe identity.
protected_difference: Recurrent-probe constraints are predecessor source carriers and obligations; they are not contract construction, comparability decision, bridge admission, wording identity test, operator identity test, or semantic authority decision procedure.
discriminator: Contrast source-bound contract and standing bridge data with a foil that equates wording with probe identity, treats an operator identifier as comparability, ignores changed contract dimensions, or admits a bridge without standing typed correspondence; retain only the exact source-supported recurrent-probe boundary after regeneration and independent checking.
horizon: Recurrent-probes boundary only; operator occurrence index is fixed input, contract/bridge construction, comparability/identity decision, standing admission, semantic authority, and all unrelated material remain explicit unless independently formalized and checked, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014, D-9015, D-9016, D-9017, D-9018, D-9019, D-9020, D-9021
relevant_failures: F-9006, F-9007
if_pass: retain the smallest checked operator-occurrence-index boundary and then elaborate recurrent probes
if_fail: reopen the earliest state, operator, return, boundary, event reference, lookup, snapshot, history, obligation, or source boundary relation; do not substitute proofs, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct a wording-equals-probe, identifier-equals-comparability, changed-dimension-ignored, or unstanding-bridge
foil, then contrast it with the declared contract and bridge coordinates. Contract only after exact source regeneration
and retained obligations distinguish the smallest recurrent-probe boundary.

## Prohibitions at this boundary

- Do not construct contracts or bridges, decide comparability, or use wording or identifier equality as a probe identity test.
- Do not treat a changed binding, grain, comparator, applicability, or protected meaning as comparable without a standing bridge.
- Do not use axioms, proof gaps, or Rust behavior to bypass schema typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest recurrent-probe boundary, explicit source obligations, and independent breakers, then
select the next residual. On failure, preserve the return and reopen the earliest contract, dimension, wording,
identifier, bridge, comparability, standing, or source boundary relation.
