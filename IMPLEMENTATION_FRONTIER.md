# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-PROGRAM-CORE-GRAMMAR-001` machine-checks first-order Return, nonempty finite Branch,
and raw Probe syntax while retaining interpretation and typing as predecessor obligations. It keeps
event handling, continuation semantics, execution, solver, and successor meanings open; Gate B remains pending.

## Strongest live obligation

Elaborate source-safe Ask lowering while preserving source configuration, occurrence identity, typed port evidence, raw event routing, and resolver outcomes without direct answer-slot filling.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-SOURCE-SAFE-ASK-LOWERING
plan_phase: B
goal: Determine the smallest source-regenerative boundary for v2.0 source-safe Ask lowering, preserving occurrence-specific port evidence, ordinary event routing, resolver outcomes, and the source-continuation gate.
protected_difference: A raw Probe return remains lower-level event evidence; it cannot directly fill a source Ask answer slot or invoke a source continuation without the declared resolver outcome.
discriminator: Contrast the source-bound lowering factorization with a direct raw-handler-to-answer-slot foil; retain only the exact source-supported occurrence, port, event, raw, route, resolver, and outcome boundary after regeneration and independent checking.
horizon: Source-safe Ask-lowering boundary only; program-core grammar is fixed input, source semantics, event actuality, route validity, resolver computation, compilation, execution, answer semantics, and all unrelated LegacyObligations remain explicit unless independently formalized and checked, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014, D-9015, D-9016, D-9017, D-9018, D-9019, D-9020, D-9021
relevant_failures: F-9006, F-9007
if_pass: retain the smallest checked program-core-grammar boundary and then elaborate source-safe Ask lowering
if_fail: reopen the earliest constructor, first-order syntax, raw-continuation, obligation, or source boundary relation; do not substitute proofs, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct a direct raw-handler-to-source-answer-slot foil, then contrast it with occurrence-specific port evidence,
ordinary event and raw-return identity, route provenance, resolver outcome, and source-continuation gate. Contract only
after exact source regeneration and retained obligations distinguish the smallest source-safe Ask-lowering boundary.

## Prohibitions at this boundary

- Do not let a lower-level raw handler fill a source answer slot or invoke a source continuation directly.
- Do not treat the lowering carrier as execution, answer selection, a solver, or successor semantics.
- Do not use axioms, proof gaps, or Rust behavior to bypass schema typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest source-safe Ask-lowering boundary, explicit source obligations, and independent breakers,
then select the next residual. On failure, preserve the return and reopen the earliest occurrence, port, event, route,
resolver, continuation, or source boundary relation.
