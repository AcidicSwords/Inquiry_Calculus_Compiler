# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-BINDING-TYPE-001` regenerates seven exact v2.0 source identities into a ten-slot binding
surface, fifteen binding-indexed type-code constructors, and three retained grammar obligations.
The independent checker rejects fourteen source, representation, and promotion mutations; Gate B
remains pending.

## Strongest live obligation

Elaborate represented forms against the checked binding/type surface without treating an arbitrary
host value as a form, silently choosing relation semantics, or consuming the later relation layer.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-REPRESENTED-FORMS
plan_phase: B
goal: Reconstruct the explicit v2.0 represented-form surface as binding-typed data against TypeCode and admissibility, while retaining the exact formal/obligation source split and postponing relation schemas, formulas, questions, and successor primitive choice.
protected_difference: A represented form must carry the binding and predecessor type code under which it is admitted; raw host values erase that boundary, while prematurely embedding relation semantics consumes later source layers and misstates the predecessor surface.
discriminator: Construct an overcomplete binding-indexed form carrier and admissibility boundary from every explicit form source, contrast it with untyped host values and forms that carry relation semantics, then contract only after exact source regeneration, type preservation, and source-bound obligation retention distinguish the smallest responsible representation.
horizon: Represented forms only; binding/type artifacts are fixed inputs, relations and relation expressions remain open, classicality and all unrelated LegacyObligations remain explicit, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013
relevant_failures: F-9006, F-9007
if_pass: retain the typed represented-form carrier and source obligations, then elaborate typed relations against it
if_fail: reopen the earliest form identity, binding/type attachment, source-boundary, or semantics-contamination relation; do not substitute raw host values, unchecked casts, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct every explicit represented-form alternative as an overcomplete binding-indexed carrier,
then contrast it with untyped host values and a form that already embeds relation semantics. Contract
only after exact source regeneration and type-preserving admission distinguish which data belongs to
the form layer rather than the later relation layer.

## Prohibitions at this boundary

- Do not treat bare host values as represented forms.
- Do not place relation, formula, question, or program semantics in the form carrier.
- Do not use strings, unchecked casts, axioms, proof gaps, or Rust types to bypass form typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest source-regenerative represented-form carrier, typed admission boundary,
explicit unresolved obligations, and independent breakers, then select typed relations. On failure,
preserve the return and reopen the earliest form, binding/type, source, or semantics-boundary relation.
