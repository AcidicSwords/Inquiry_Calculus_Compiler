# Implementation Frontier

This file is the sole live implementation cursor. Historical work belongs in Git, demonstrated behavior in `CONFORMANCE_STATUS.md`, and accepted choices or failures in the JSONL ledgers.

Repository actuality must be probed before acting. The current residual was derived from demonstrated implementation behavior through predecessor coordinate `ca5582e`; that coordinate is ancestry, not a claim that a tracked document can name its own final commit or worktree state.

## Immediate demonstrated predecessor

`QCONVERSE-NOT-INVERSE-001` established under exhaustive finite coverage that a many-to-one relation's converse is the complete same-use reverse section. Either member may be selected while both selections retain the same two-member fiber; selection does not create or prove a single-valued inverse. This adds no inverse opcode, controller, scheduler, canonical artifact, table, or second history.

## Strongest live obligation

Require an explicit checked binding-supplied law before treating a forward/backward pair as an adjunction or weakest-condition transformer.

<!-- LIVE_FRONTIER_BEGIN -->
id: QADJOINT-001
plan_phase: 10
goal: Admit a finite forward/backward pairing as an adjunction or weakest-condition transformer only through an explicit binding-supplied law checked over its entire declared finite domain.
protected_difference: A converse or backward-looking relation supplies existential preimages; it does not supply the universal equivalence required by an adjunction or weakest-condition law.
discriminator: Construct one finite pair that satisfies an explicit pointwise adjunction equivalence and one structural-converse foil that violates it at a named input; admit only the first and retain the counterexample for the second.
horizon: one explicit binding, two finite domains, one declared comparison relation, exhaustive pointwise checking, no universal cross-binding theorem
relevant_decisions: D-0024, D-0097, D-0098, D-0127, D-0135, D-0139, D-0140
relevant_failures: F-0003
if_pass: QRECIP-PROV-001 reject a generic backward route whose use identity differs from the exterior-producing negation use
if_fail: reopen the smallest binding/domain/comparison/forward/backward/law-coverage distinction; do not infer a law from naming, converse shape, samples, or endpoints
<!-- LIVE_FRONTIER_END -->

## Smallest decisive fixture

Start from two declared finite domains and one explicit comparison relation. Supply one forward and one backward table plus the binding's exact adjunction law. Exhaustively check the law at every domain pair and retain the exact binding and coverage.

Construct a foil with the same types and backward-looking structural shape but change one table entry so the law fails at a named pair. The checker must reject it with that counterexample. The fixture is local binding-supplied structure; it must not require a controller, scheduler, global transition, or new history.

The smallest wrong implementation accepts a forward/backward pair because it is named an adjunction or resembles a converse without checking the supplied law exhaustively.

## Prohibitions at this boundary

- Do not treat converse orientation, backward wording, common endpoints, generated routes, or sample agreement as an adjunction or weakest-condition guarantee.
- Do not infer universal law coverage from absent counterexamples under partial search; retain `Unknown` outside the declared finite domain.
- Do not synthesize an adjoint opcode, controller, scheduler, global policy, authoritative table, or second history to make the local case pass.

## Exit

On pass, update conformance and any warranted append-only decision, replace the live block with `QRECIP-PROV-001`, and continue. On failure, preserve the actual return and reopen the earliest implicated binding/domain/comparison/law relation rather than broadening the architecture.
