# Implementation Frontier

This file is the sole live implementation cursor. Historical work belongs in Git, demonstrated behavior in `CONFORMANCE_STATUS.md`, and accepted choices or failures in the JSONL ledgers.

Repository actuality must be probed before acting. The current residual was derived from demonstrated implementation behavior through predecessor coordinate `62cc2ca`; that coordinate is ancestry, not a claim that a tracked document can name its own final commit or worktree state.

## Immediate demonstrated predecessor

`QACTUAL-SEPARATION-001` established source-linked ordinary actuality without a second history. Legacy event-v1 identities remain exactly readable; source-derived dispatches use event v2 to retain exact `AskOccurrence` and compiler references. Cold replay re-walks two protected-different source occurrences sharing request, question, operator, raw bytes, raw identity, and admitted candidates, then reaches their distinct continuations with no redispatch. A finite all-Probe discharge bundle retains exact port-indexed routes and resolution paths; one event may cover multiple ports only through an exact explicit shared-event admission. This adds no occurrence table, port-evidence table, scheduler, controller, opcode, or causal-order claim.

## Strongest live obligation

Preserve every tagged path and each child port's authority through a non-singleton dependent continuation instead of selecting one completion or collapsing a mixed-mode family.

<!-- LIVE_FRONTIER_BEGIN -->
id: QLIFT-ALLPATHS-001
plan_phase: 3,4
goal: Derive one finite dependent continuation that preserves every tagged member of two non-singleton supported stages and each child port's independent discharge authority.
protected_difference: A whole supported answer is a nonempty set with per-member evidence, and a dependent next stage may expose several tagged children with different Probe, Check, or Warrant obligations. Choosing one member, dropping path tags, merging equal endpoints, or replacing component modes with one common mode changes protected continuation authority.
discriminator: Construct two finite non-singleton stages whose cartesian dependent family has protected-different tagged paths and independently supported child ports. Admit the complete materialization, reconstruct every continuation path, and reject first-member selection, missing or duplicated tags, swapped child authority, and common-mode collapse. Remove at least one materialized member while retaining a binding-native availability declaration and require the uncovered region to remain Unknown rather than exact-empty, negative, or complete.
horizon: two exact finite two-member stages, one complete four-path dependent family, one proper submaterialization, finite Probe/Check child authorities, and structural path equality; no intensional enumeration theorem, general monad implementation, provider scheduling, or global closure
relevant_decisions: D-0048, D-0104, D-0108, D-0110, D-0128, D-0131, D-0150
relevant_failures: F-0001
if_pass: QRESOLUTION-GATE-001 preserve Supported, ExactEmpty, Undefined, Unsupported, and Unknown through non-singleton relational resolution
if_fail: reopen the earliest answer-set, tag, dependent-family, child-authority, or materialization-coverage relation; do not invent singleton selection, semantic monad authority, scheduler, controller, table, or opcode
<!-- LIVE_FRONTIER_END -->

## Smallest decisive fixture

Start with two exact two-member supported stages. Give each first-stage member a stable tag and map it to a two-member dependent child field whose elements retain their parent tag, child tag, answer member, occurrence/continuation route, and per-port discharge authority. The complete fixture must expose all four paths even when two endpoints or modes coincide.

Then remove one child from the represented materialization while separately retaining a binding-native declaration that the child family may contain more members. The result must name the uncovered tag/member region as `Unknown`; it may not silently shrink the family, select a representative, or claim exact emptiness.

The smallest wrong implementation uses iterator-first selection, a set keyed only by endpoint, an untagged flatten, or one common discharge mode. Each loses a path or changes which authority can resume a child continuation.

## Prohibitions at this boundary

- Do not select a singleton from a non-singleton supported answer or dependent child family.
- Do not erase parent/child tags, per-member support, or per-port mode and authority merely because endpoints coincide.
- Do not turn incomplete materialization into exact emptiness, unsupported, negative, or complete coverage.
- Do not add a semantic monad primitive, scheduler, controller, table, opcode, or canonical dependent-family artifact before a breaker proves transparent composition over ordinary typed relations and first-order programs insufficient.

## Exit

On pass, update conformance and any warranted append-only decision, replace the live block with `QRESOLUTION-GATE-001`, and continue. On failure, preserve the actual return and reopen the earliest implicated answer-set/tag/dependent-family/child-authority/materialization relation rather than broadening the architecture.
