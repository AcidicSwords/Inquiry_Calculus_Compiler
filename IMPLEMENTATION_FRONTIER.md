# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-QUESTION-REFINEMENT-SEMANTICS-001` machine-checks the sufficient factorization-to-local
kernel-inclusion direction and makes the reverse construction explicitly conditional on profile
coverage. The independent checker rejects eleven source, coverage, promotion, carrier, global-order,
program, and axiom mutations; Gate B remains pending.

## Strongest live obligation

Elaborate joint and active refinement without treating their displayed kernel equations as proved
global representation laws or erasing their dependency on functional profile coordinates.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-QUESTION-REFINEMENT-JOINT-AND-ACTIVE
plan_phase: B
goal: Determine the smallest source-regenerative boundary for the v2.0 joint and active refinement equations, preserving all unproved global representation, successor-primitive, and Rust meaning as open unless independently established.
protected_difference: A joint or active coordinate can be typed locally; its displayed kernel intersection is neither a global representation theorem nor an automatic consequence outside functional profile scope.
discriminator: Contrast a source-bound joint/active candidate relation with a premature global kernel theorem, representation promise, program, and omission; retain only the exact source-supported boundary after regeneration and independent checking.
horizon: Joint and active refinement boundary only; local preorder and conditional factorization/kernel syntax are fixed inputs, unconditional joint-kernel equality, nonredundancy, active-representation semantics, actual execution, and all unrelated LegacyObligations remain explicit unless their exact source classification and independent checks establish otherwise, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014, D-9015, D-9016, D-9017, D-9018, D-9019, D-9020, D-9021
relevant_failures: F-9006, F-9007
if_pass: retain the smallest checked joint/active boundary and then elaborate the next residual selected by its unresolved relation
if_fail: reopen the earliest joint/active source, profile, kernel, representation, or semantics/program contamination relation; do not substitute proofs, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct a decisive source-bound joint/active foil, then contrast it with premature global-kernel,
representation, program, and omission claims. Contract only after exact source regeneration and
retained obligations distinguish the smallest joint/active surface.

## Prohibitions at this boundary

- Do not treat a joint or active coordinate as a proved global kernel or representation law.
- Do not use a source display alone as a proof of joint-kernel intersection or nonredundancy.
- Do not use axioms, proof gaps, or Rust behavior to bypass schema typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest source-regenerative joint/active boundary, explicit source
obligations, and independent breakers, then select the next residual. On failure, preserve the
return and reopen the earliest joint, active, kernel, representation, program, or source boundary relation.
