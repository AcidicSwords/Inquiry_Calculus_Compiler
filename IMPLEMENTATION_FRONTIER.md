# Implementation Frontier

This file is the sole live implementation cursor. Historical work belongs in Git, demonstrated behavior in `CONFORMANCE_STATUS.md`, and accepted choices or failures in the JSONL ledgers.

Repository actuality must be probed before acting. The current residual was derived from demonstrated implementation behavior through predecessor coordinate `69a4ec3`; that coordinate is ancestry, not a claim that a tracked document can name its own final commit or worktree state.

## Immediate demonstrated predecessor

`QSUPPANS-EVIDENCE-COORD-008` moved the last unexamined coordinate of the canonical record. Holding every member, every port result, and every declared route fixed, a second declared resolution path to the same carrier and a second support environment over the same observed pairs each separate the records, and each corresponding ablation merges them. With the route determination before it, every coordinate of the canonical supported answer is now shown to participate in equality, subject to one standing limit: the record carries an occurrence that question-indexed `SuppAns(q)` does not, so it remains strictly finer than canonical supported-answer equality. Its predecessors established the mixed-mode port-evidence separation, the `Generate` proposal carrier, the port-indexed decode and resolution gates, the whole-question resolution gate, the member-set projection, and the one shared successor relation.

## Strongest live obligation

Enforce the canonical cross-port relation constraint. The resolver is required to check cross-port relation constraints and not to manufacture a Cartesian answer when the relation forbids one; D-0162 established that no relation-membership test exists anywhere in the resolution path, so that obligation is currently unenforced rather than merely untested.

<!-- LIVE_FRONTIER_BEGIN -->
id: QRESOLVE-CROSS-PORT-009
plan_phase: 8
goal: Enforce the canonical cross-port relation constraint on a whole-question answer, so a completion the relation forbids cannot be a supported member however well each port's own evidence checks out.
protected_difference: Canonical requires the resolver to check cross-port relation constraints after all components are available and not to manufacture a Cartesian answer when the relation forbids one. D-0162 established that no relation-membership test exists anywhere in this path: a completion candidate explicitly records no relation evaluation and no membership in its completion fiber, and the whole-question gate only checks that each completion agrees with each non-Probe port's own declared result. So a completion whose ports are each individually supported but whose combination the relation excludes is currently admitted as a member of a Supported answer. That is the one canonical obligation of this gate still unenforced rather than merely uncovered.
discriminator: Take one finite mixed-mode question whose relation is given by a caller-declared finite membership table, and one decoded completion field containing both an admitted tuple and a tuple the table excludes. Require the excluded completion to be refused as a member, with a residual naming the relation and the offending tuple, while the admitted one still resolves. Reject an answer whose members were never tested against the relation, a check that inspects one port at a time, a check that silently drops the offending member instead of refusing the answer, and any membership decision taken by an evaluator rather than by the caller-declared table under its own declared coverage. A table with partial coverage must leave the undecided region Unknown rather than admitted or excluded.
horizon: one finite mixed-mode occurrence, one caller-declared finite membership table over the whole port field with explicit coverage, and its existing checked contributions and member set; no general relation evaluator or search, no canonical supported-answer artifact, no second successor relation, no general executor, resolver procedure artifact, compiler procedure artifact, event for non-Probe modes, cross-binding bridge, scheduler, controller, table authority, or opcode
relevant_decisions: D-0150, D-0158, D-0159, D-0161, D-0162, D-0163, D-0165, D-0166
relevant_failures: F-0001, F-0002
if_pass: rederive the strongest remaining Phase 7/8 residual from the stable plan and pending conformance; do not reopen passed QASK/QSUCC/QREADY/QACTUAL/QRESOLUTION/QSUPPANS fixtures without a new breaker
if_fail: reopen the earliest membership-coverage, member-refusal, residual-payload, or whole-question outcome relation; do not build a general relation evaluator, a second successor authority, a dispatcher, a scheduler, a controller, or an opcode
<!-- LIVE_FRONTIER_END -->

## Smallest decisive fixture

Give the mixed relation a caller-declared finite membership table over both ports, and decode a completion field holding one tuple the table admits and one it excludes. The admitted tuple must still resolve; the excluded one must be refused with a residual naming the relation and the tuple. Then vary the table's coverage and require the undecided region to return `Unknown`.

The smallest wrong implementation admits every decoded completion because each port checked out, tests ports one at a time, drops the offending member and reports the rest as Supported, decides membership by evaluating the relation rather than reading the declared table, or treats a partially covered region as excluded.

## Prohibitions at this boundary

- Do not build a general relation evaluator, solver, or search; membership is decided only by a caller-declared finite table under its own declared coverage.
- Do not treat an uncovered region as excluded, or a failed membership lookup as negation.
- Do not introduce a canonical supported-answer artifact, envelope, or content-addressed identity for a derived record.
- Do not add a second successor relation, question history, or route authority.
- Do not introduce a general executor, compiler, resolver procedure artifact, dispatcher, scheduler, controller, table authority, or opcode.
- Do not create an event for a Pure, Generate, Check, or Warrant port merely because membership is checked.
- Do not erase exact source occurrence, port/mode, compiler, runtime-program, Probe event/route/decoder/resolution, or non-Probe typed-result/authority identities.

## Exit

On pass, update conformance and project state, then rederive the next strongest residual. On failure, preserve the actual return and reopen the earliest implicated mixed-port relation rather than broadening the architecture.
