# Implementation Frontier

This file is the sole live implementation cursor. Historical work belongs in Git, demonstrated behavior in `CONFORMANCE_STATUS.md`, and accepted choices or failures in the JSONL ledgers.

Repository actuality must be probed before acting. The current residual was derived from demonstrated implementation behavior through predecessor coordinate `f0f313a`; that coordinate is ancestry, not a claim that a tracked document can name its own final commit or worktree state.

## Immediate demonstrated predecessor

`QLIFT-ALLPATHS-001` established the finite derived `LiftQ_F` boundary over ordinary proof-carrying answers and checked questions. One exact two-member supported answer lifts to two parent-indexed child questions and four distinct `(parent, port)` positions carrying Probe, Check, Generate, and Warrant modes with their own type and route context. Exact parent coverage returns a complete family; a proper materialization retains every missing parent as `Unknown`; empty, duplicate-parent, and foreign-parent materializations reject. This establishes question-family construction, not child-answer execution, source lowering, a scheduler, a semantic monad primitive, or a canonical dependent-family artifact.

## Strongest live obligation

Preserve the five resolution outcomes and every compatible output of a finite non-singleton relational run, while allowing only a supported whole answer to reach its exact source occurrence continuation.

<!-- LIVE_FRONTIER_BEGIN -->
id: QRESOLUTION-GATE-001
plan_phase: 7,8
goal: Derive one finite checked resolution gate that keeps Supported, ExactEmpty, Undefined, Unsupported, and Unknown distinct and preserves every compatible output of a non-singleton relational Run/Compose path.
protected_difference: Exact exhaustion, malformed or undefined decoding, decoded but unsupported candidates, incomplete coverage, and a proof-carrying nonempty answer authorize different continuations. Selecting the first compatible result or letting any non-Supported outcome construct `ResolvedOccurrenceIR` changes the answer set, evidence boundary, and source occurrence continuation.
discriminator: Drive one declared finite question through all five outcomes with distinct payloads and evidence. Give the relational resolver at least two compatible outputs and require both to survive canonical finite ordering and exact support admission. Re-walk the exact source Ask occurrence and prove that only `Supported(S_hat)` can construct its resolved occurrence and resume its continuation. Reject first-output selection, malformed or undefined results, decoded-but-unsupported candidates, incomplete coverage, swapped occurrence/continuation identity, and outcome collapse.
horizon: one finite relation and source Ask, one exhaustively empty run, one undefined decode, one unsupported decoded field, one incomplete field, one non-singleton supported field, exact finite coverage, and structural occurrence/continuation equality; no general search-completeness theorem, provider scheduler, intensional relation engine, cross-binding transport, or global resolution policy
relevant_decisions: D-0113, D-0114, D-0120, D-0128, D-0150, D-0151
relevant_failures: F-0001, F-0002
if_pass: QCODE-TYPING-001 preserve source/runtime quotation result types and binding/compiler-version-indexed interpretation
if_fail: reopen the earliest decode-outcome, relation-run, coverage, support-admission, occurrence, or continuation gate; do not weaken outcome distinctions or add a scheduler, controller, table, opcode, or implicit singleton policy
<!-- LIVE_FRONTIER_END -->

## Smallest decisive fixture

Construct one finite resolver whose declared candidate domain admits two compatible outputs for the same relation use. Preserve the whole finite output field and admit it only through exact decoded-observation and standing-support routes. Then vary one boundary at a time to obtain exact emptiness, undefined decoding, decoded-but-unsupported candidates, and incomplete coverage, each with its own payload and no resumption authority.

Tie the Supported result to one freshly re-walked source `AskOccurrence` and construct `ResolvedOccurrenceIR` only after exact question, binding, compiler, provenance, answer, and continuation checks. The smallest wrong implementation uses iterator-first selection, aliases empty/undefined/unsupported/unknown, or accepts an equal question/endpoint from another occurrence.

## Prohibitions at this boundary

- Do not select the first output of a non-singleton relational result or supported answer.
- Do not collapse ExactEmpty, Undefined, Unsupported, Unknown, and Supported or let one outcome borrow another's continuation authority.
- Do not construct a resolved occurrence from decoding, generation, declaration, or endpoint equality without exact support and source-occurrence checks.
- Do not add a general relation engine, search-completeness claim, scheduler, controller, table, opcode, or singleton policy before the finite ordinary composition fails a protected breaker.

## Exit

On pass, update conformance and any warranted append-only decision, replace the live block with `QCODE-TYPING-001`, and continue. On failure, preserve the actual return and reopen the earliest implicated decode-outcome/relation-run/coverage/support/occurrence/continuation relation rather than broadening the architecture.
