# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-DISCHARGE-ROUTE-001` machine-checks the declared per-port mode plus carrier, result,
continuation, mixed-route, and non-dispatch constraints as source obligations. It keeps route selection,
execution, answer construction, and successor meanings open; Gate B remains pending.

## Strongest live obligation

Elaborate the LLM possibility relation while keeping the latent possibility field distinct from realized prompt/response history and without claiming direct symbolic access.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-LLM-POSSIBILITY-RELATION
plan_phase: B
goal: Determine the smallest source-regenerative boundary for the v2.0 binding-level LLM possibility relation, preserving its context, prompt, budget, raw-completion shape, parameterized latent field, and separation from realized history.
protected_difference: The possibility relation is a predecessor obligation; it is not a latent-field implementation, direct symbolic-access claim, completion executor, history constructor, or semantic authority decision procedure.
discriminator: Contrast the source-bound relation and weights/trace separation with a foil that equates latent possibility to realized history or constructs/directly accesses the latent field; retain only the exact source-supported relation after regeneration and independent checking.
horizon: LLM-possibility-relation boundary only; Stage 4 discharge-route selection is fixed input, latent-field behavior, prompt execution, raw completion interpretation, history construction, semantic authority, and all unrelated LegacyObligations remain explicit unless independently formalized and checked, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014, D-9015, D-9016, D-9017, D-9018, D-9019, D-9020, D-9021
relevant_failures: F-9006, F-9007
if_pass: retain the smallest checked Stage 4 discharge-route boundary and then elaborate the LLM possibility relation
if_fail: reopen the earliest port, mode, carrier, result, continuation, execution, obligation, or source boundary relation; do not substitute proofs, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct a latent-field-as-realized-history or direct-symbolic-access foil, then contrast it with the declared
context/prompt/budget-to-raw-completion relation and weights/trace separation. Contract only after exact source
regeneration and retained obligations distinguish the smallest LLM-possibility boundary.

## Prohibitions at this boundary

- Do not implement, directly access, or equate the latent possibility field with realized history.
- Do not turn the relation into prompt execution, raw-completion interpretation, or history construction.
- Do not use axioms, proof gaps, or Rust behavior to bypass schema typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest LLM-possibility-relation boundary, explicit source obligations, and independent breakers,
then select the next residual. On failure, preserve the return and reopen the earliest relation, latent-field,
history-separation, access, execution, or source boundary relation.
