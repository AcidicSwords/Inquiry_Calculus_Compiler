# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-ACTIVE-VIEW-001` machine-checks the view shape plus inclusion, reachable-reserve,
licensed-occlusion, and reopening conditions as source obligations. It keeps view construction,
relevance/licence decisions, execution, solver, and successor meanings open; Gate B remains pending.

## Strongest live obligation

Elaborate Stage 4 discharge-route selection while preserving per-port mode, typed carrier, and answer-dependent continuation constraints without dispatch execution or answer construction.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-DISCHARGE-ROUTE-SELECTION
plan_phase: B
goal: Determine the smallest source-regenerative boundary for v2.0 Stage 4 discharge-route selection, preserving every open port's declared mode, typed carrier, typed result, and answer-dependent continuation.
protected_difference: Discharge-route constraints are predecessor obligations; they are not yet a dispatcher, evidence executor, answer constructor, or semantic authority decision procedure.
discriminator: Contrast source-bound per-port route carrier with mode collapse, untyped result, opaque continuation, dispatch execution, or answer manufacture; retain only the exact source-supported Stage 4 boundary after regeneration and independent checking.
horizon: Discharge-route-selection boundary only; Stage 3 active view is fixed input, route dispatch, evidence execution, answer interpretation, continuation execution, semantic authority, and all unrelated LegacyObligations remain explicit unless independently formalized and checked, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014, D-9015, D-9016, D-9017, D-9018, D-9019, D-9020, D-9021
relevant_failures: F-9006, F-9007
if_pass: retain the smallest checked Stage 3 active-view boundary and then elaborate Stage 4 discharge-route selection
if_fail: reopen the earliest view shape, inclusion, reserve, licence, reopening, obligation, or source boundary relation; do not substitute proofs, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct a mode-collapsing or answer-manufacturing dispatcher foil, then contrast it with each port's declared mode,
typed carrier/result, and answer-dependent continuation. Contract only after exact source regeneration and retained
obligations distinguish the smallest Stage 4 discharge-route-selection boundary.

## Prohibitions at this boundary

- Do not turn route constraints into dispatch execution, answer construction, or continuation execution.
- Do not collapse declared modes, typed carriers/results, or answer-dependent continuation data.
- Do not use axioms, proof gaps, or Rust behavior to bypass schema typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest Stage 4 discharge-route-selection boundary, explicit source obligations, and independent
breakers, then select the next residual. On failure, preserve the return and reopen the earliest port, mode, carrier,
result, continuation, execution, or source boundary relation.
