# Implementation Frontier

This file is the sole live implementation cursor. Historical work belongs in Git, demonstrated behavior in `CONFORMANCE_STATUS.md`, and accepted choices or failures in the JSONL ledgers.

Repository actuality must be probed before acting. The current residual was derived from demonstrated implementation behavior through predecessor coordinate `69a4ec3`; that coordinate is ancestry, not a claim that a tracked document can name its own final commit or worktree state.

## Immediate demonstrated predecessor

`QSUPPANS-RECORD-IDENTITY-007` determined by construction that the route projection is retained rather than missing: on the non-Probe side as a field that participates in record equality, on the Probe side through the retained event whose route the bundle already checked. Two answers over one occurrence agreeing on every member and every port result but differing in one declared route compare unequal while their member projections compare equal, and removing route from equality merges them. Two limits were recorded rather than closed: the evidence-environment coordinate was never varied independently, and the record is strictly finer than question-indexed `SuppAns(q)` because it carries an occurrence, so this is not yet canonical supported-answer equality. Its predecessors established the mixed-mode port-evidence separation, the `Generate` proposal carrier, the port-indexed decode and resolution gates, the whole-question resolution gate, the member-set projection, and the one shared successor relation.

## Strongest live obligation

Determine whether the evidence-environment coordinate participates in supported-answer equality, by varying it independently of route and of every port result — the one coordinate of the canonical record that no construction has yet moved.

<!-- LIVE_FRONTIER_BEGIN -->
id: QSUPPANS-EVIDENCE-COORD-008
plan_phase: 8
goal: Determine whether the evidence-environment coordinate of a supported answer participates in record equality, by varying it independently of route and of every port's contributed result.
protected_difference: Canonical SuppAns sums over a route and an evidence environment before its member set and support witness, and equality is equality of the whole record. Route has now been shown retained and separating. Evidence has not been moved by any construction: on the Probe side it is the event, raw return, observations, and resolved support inside the admitted answer set, and on the non-Probe side it is the typed output and resolution path. If some part of that is absent from the record or absent from its equality, two answers supported through different evidence environments would compare equal, and support failure, reopening, and standing decisions keyed on answer equality would treat distinct support as one.
discriminator: Construct two whole-question answers over one occurrence that agree on every member, every port result, and every declared route, but reach support through a different evidence environment - a different support environment closing the same observations, or a different resolution path carrying the same typed result to the same port carrier. Require the records to compare unequal. Reject an equality that ignores a protected evidence difference, and reject a construction whose evidence difference is not independently checked against its own port evidence rather than merely declared.
horizon: one finite mixed-mode occurrence, its existing checked contributions and member set, and finite caller-declared support environments and resolution paths; no canonical supported-answer artifact or envelope, no second successor relation, no general executor, resolver procedure artifact, compiler procedure artifact, event for non-Probe modes, cross-binding bridge, scheduler, controller, table, or opcode
relevant_decisions: D-0150, D-0156, D-0158, D-0159, D-0160, D-0161, D-0162, D-0163, D-0165
relevant_failures: F-0001, F-0002
if_pass: rederive the strongest remaining Phase 7/8 residual from the stable plan and pending conformance; do not reopen passed QASK/QSUCC/QREADY/QACTUAL/QRESOLUTION/QSUPPANS fixtures without a new breaker
if_fail: reopen the earliest evidence-environment, support-witness, route-projection, or record-equality relation; do not introduce a canonical supported-answer artifact, a second successor authority, a general executor, a dispatcher, a scheduler, a controller, a table, or an opcode
<!-- LIVE_FRONTIER_END -->

## Smallest decisive fixture

Reuse the rerouted-answer construction, but hold the route fixed and move the evidence instead: admit the same members and the same port results through a second support environment, or carry the same typed result to the same carrier along a second resolution path. Require the records to separate. If they do not, name exactly which part of the evidence environment failed to reach the record.

The smallest wrong implementation compares answers by member projection, accepts a declared evidence coordinate without rechecking it against the port evidence it claims to summarize, varies evidence and route together so neither is isolated, or introduces a canonical supported-answer artifact to force equality by content address.

## Prohibitions at this boundary

- Do not introduce a canonical supported-answer artifact, envelope, or content-addressed identity for a derived record.
- Do not add a second successor relation, question history, or route authority.
- Do not introduce a general executor, compiler, resolver procedure artifact, dispatcher, scheduler, controller, table, or opcode.
- Do not create an event for a Pure, Generate, Check, or Warrant port merely because a record is compared.
- Do not erase exact source occurrence, port/mode, compiler, runtime-program, Probe event/route/decoder/resolution, or non-Probe typed-result/authority identities.

## Exit

On pass, update conformance and project state, then rederive the next strongest residual. On failure, preserve the actual return and reopen the earliest implicated mixed-port relation rather than broadening the architecture.
