# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-RESIDUAL-AMBIGUITY-001` machine-checks two field members in distinct declared
horizon-relative classes. It keeps separator validity, answer selection, solver, execution, and
successor meanings out of the boundary; Gate B remains pending.

## Strongest live obligation

Elaborate representation defect without treating a same-representation/different-class witness as a
separator, answer, solver, program behavior, or successor semantics.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-REPRESENTATION-DEFECT
plan_phase: B
goal: Determine the smallest source-regenerative boundary for v2.0 representation defect, preserving a representation map, equal representation cell, horizon-relative non-equivalence, protected consequence relation, separator validity, answer semantics, successor primitives, and Rust meaning as open unless independently established.
protected_difference: A representation defect has two forms sharing one current representation but differing in protected class; it is distinct from a successful separator, answer, solver, program, or successor semantics.
discriminator: Contrast a source-bound equal-representation/non-equivalent pair witness with literal inequality, equal protected class, successful separator, selected-answer, solver, program, semantic-evaluation, and omission claims; retain only the exact source-supported defect boundary after regeneration and independent checking.
horizon: Representation-defect boundary only; residual ambiguity is fixed input, horizon equivalence laws, consequence relation meaning, separator validity, answer semantics, execution, and all unrelated LegacyObligations remain explicit unless their exact source classification and independent checks establish otherwise, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014, D-9015, D-9016, D-9017, D-9018, D-9019, D-9020, D-9021
relevant_failures: F-9006, F-9007
if_pass: retain the smallest checked representation-defect boundary and then elaborate the next residual selected by its unresolved relation
if_fail: reopen the earliest representation, cell, protected class, separator, answer, program, or source boundary relation; do not substitute proofs, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct a decisive source-bound same-cell/different-class foil, then contrast it with literal
inequality, equal protected class, successful separator, selected-answer, solver, program,
semantic-evaluation, and omission claims. Contract only after exact source regeneration and retained
obligations distinguish the smallest defect boundary.

## Prohibitions at this boundary

- Do not treat a representation defect as a successful separator, selected answer, total solver, program, or successor semantics.
- Do not use a source definition alone as a context-separation, answer-validity, or solver result.
- Do not use axioms, proof gaps, or Rust behavior to bypass schema typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest source-regenerative representation-defect boundary, explicit source
obligations, and independent breakers, then select the next residual. On failure, preserve the
return and reopen the earliest representation, cell, protected-class, separator, answer, program,
or source boundary relation.
