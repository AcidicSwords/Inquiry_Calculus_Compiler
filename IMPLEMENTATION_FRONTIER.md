# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-QUESTION-REFINEMENT-PREORDER-001` regenerates the three v2.0 refinement declarations
into typed local candidate carriers. The independent checker rejects eleven source, promotion,
carrier, obligation, semantic/kernel, program, and axiom mutations; Gate B remains pending.

## Strongest live obligation

Elaborate the first question-refinement semantic relation only after determining which source-bound
profile, factorization, kernel, and nonredundancy claims can be represented without promotion.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-QUESTION-REFINEMENT-SEMANTICS
plan_phase: B
goal: Determine the smallest source-regenerative formal boundary for the v2.0 question-refinement semantic claims, preserving every unproved law, global representation, successor-primitive, and Rust meaning as open unless independently established.
protected_difference: A refinement semantic claim requires a typed profile and explicit dependency direction; it is neither supplied by a local candidate carrier nor established merely by a syntactic coordinate.
discriminator: Contrast a source-bound candidate semantic obligation with a premature factorization/kernel theorem, global representation promise, program, and omission; retain only the exact source-supported boundary after regeneration and independent checking.
horizon: Question-refinement semantic boundary only; local carrier syntax is fixed input, functional factorization, kernel inclusion/intersection, nonredundancy, active-representation semantics, actual execution, and all unrelated LegacyObligations remain explicit unless their exact source classification and independent checks establish otherwise, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014, D-9015, D-9016, D-9017, D-9018, D-9019, D-9020, D-9021
relevant_failures: F-9006, F-9007
if_pass: retain the smallest checked refinement-semantic boundary and then elaborate the next residual selected by its unresolved relation
if_fail: reopen the earliest refinement source, profile, factorization, kernel, or semantics/program contamination relation; do not substitute proofs, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct a decisive source-bound semantic foil for the local refinement carriers, then contrast it
with premature factorization, kernel, global-representation, program, and omission claims. Contract
only after exact source regeneration and retained obligations distinguish the smallest semantic surface.

## Prohibitions at this boundary

- Do not treat a candidate semantic obligation as a proved preorder, factorization, kernel law, or global representation.
- Do not use a local coordinate or a source display alone as a proof of joint-kernel intersection or nonredundancy.
- Do not use axioms, proof gaps, or Rust behavior to bypass schema typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest source-regenerative refinement-semantic boundary, explicit source
obligations, and independent breakers, then select the next residual. On failure, preserve the
return and reopen the earliest profile, factorization, kernel, representation, program, or source boundary relation.
