# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-REPRESENTATION-SEARCH-001` machine-checks witnessed protected difference, candidate
forms, binding order, consequence relevance, and excess-detail ablation boundary. It keeps
equivalence, candidate execution, relevance proof, solver, and successor meanings open; Gate B remains pending.

## Strongest live obligation

Elaborate the program-core grammar while preserving Return, Branch, and raw Probe continuation syntax without execution or hidden closure semantics.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-PROGRAM-CORE-GRAMMAR
plan_phase: B
goal: Determine the smallest source-regenerative boundary for v2.0 program-core grammar, preserving Return, Branch, raw Probe continuation, and syntax/interpreter separation.
protected_difference: Core program syntax is a first-order carrier; raw Probe continuation is not execution, a hidden closure, a solver, or successor semantics.
discriminator: Contrast source-bound core grammar with execution, hidden closure, answer selection, solver, and semantic-evaluation claims; retain only the exact source-supported grammar boundary after regeneration and independent checking.
horizon: Program-core-grammar boundary only; representation search is fixed input, interpreter, raw event handling, execution, answer semantics, and all unrelated LegacyObligations remain explicit unless independently formalized and checked, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014, D-9015, D-9016, D-9017, D-9018, D-9019, D-9020, D-9021
relevant_failures: F-9006, F-9007
if_pass: retain the smallest checked representation-search boundary and then elaborate the next residual selected by its unresolved relation
if_fail: reopen the earliest witness, candidate, relevance, ablation, or source boundary relation; do not substitute proofs, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct a decisive source-bound execution or hidden-closure foil, then contrast it with Return, Branch, and raw
Probe continuation syntax as first-order data. Contract only after exact source regeneration and retained obligations
distinguish the smallest program-core-grammar boundary.

## Prohibitions at this boundary

- Do not execute raw Probe continuation or treat syntax as a hidden closure, answer selection, or solver.
- Do not treat this carrier as an interpreted program or successor semantics.
- Do not use axioms, proof gaps, or Rust behavior to bypass schema typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest program-core-grammar boundary, explicit source obligations, and independent breakers,
then select the next residual. On failure, preserve the return and reopen the earliest syntax, continuation,
interpretation, or source boundary relation.
