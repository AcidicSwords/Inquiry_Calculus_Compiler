# Implementation Frontier

This file is the sole live implementation cursor. Historical work belongs in Git, demonstrated behavior in `CONFORMANCE_STATUS.md`, and accepted choices or failures in the JSONL ledgers.

Repository actuality must be probed before acting. The current residual was derived from demonstrated implementation behavior through predecessor coordinate `0318e97`; that coordinate is ancestry, not a claim that a tracked document can name its own final commit or worktree state.

## Immediate demonstrated predecessor

`QASK-MIXED-MODE-004` established a finite occurrence-indexed mixed discharge field with exactly one source-linked Probe component and typed non-Probe components. The Probe side retains event/route/resolution evidence; non-Probe evidence carries its typed value, declared mode, route, binding, compiler, provenance, support, and warrant, and cannot contain an event. It rejects port/mode/type/authority substitutions without dispatch. No general executor, event for non-Probe modes, table, or opcode was added.

## Strongest live obligation

Connect the finite mixed discharge field to the checked source Ask runtime lowering, preserving exact occurrence, complete port/mode field, compiler coordinate, and runtime identity without turning the field into a compiler or executor.

<!-- LIVE_FRONTIER_BEGIN -->
id: QASK-MIXED-LOWERING-005
plan_phase: 5
goal: Derive the narrow pairing that admits a finite mixed discharge field only for the exact checked SourceAskLowering it discharges, preserving source occurrence, full port/mode field, compiler coordinate, and runtime identity.
protected_difference: A source lowering and a mixed field can each be valid while referring to distinct source occurrences with compatible question/result/endpoint projections. Pairing by a projection permits the wrong runtime continuation or evidence field to enter a source control path.
discriminator: Construct one source Ask with Probe and Generate ports, one checked runtime artifact, and one mixed field. Accept only their exact occurrence, port/mode set, compiler, and runtime identities; reject a substituted occurrence, runtime, field port/mode, or non-Probe authority. Verification must not dispatch.
horizon: one finite mixed-mode source Ask, one checked runtime artifact, one mixed field, and no dispatch; no compiler procedure, general executor, resolver procedure artifact, event for non-Probe modes, cross-binding bridge, scheduler, controller, table, or opcode
relevant_decisions: D-0125, D-0156, D-0157, D-0158, D-0159
relevant_failures: F-0001, F-0002
if_pass: rederive the strongest remaining Phase 5/6 residual from the stable plan and pending conformance; do not reopen passed QASK/QSUCC/QREADY/QACTUAL fixtures without a new breaker
if_fail: reopen the earliest mixed field/lowering occurrence, port/mode, compiler, runtime, typed-result, or authority-route relation; do not build a general compiler, executor, dispatcher, scheduler, controller, table, or opcode
<!-- LIVE_FRONTIER_END -->

## Smallest decisive fixture

Pair one checked `SourceAskLowering` with one checked finite mixed field. Rewalk both operands and compare their exact source occurrence, compiler coordinate, and complete port/mode field; retain runtime identity from the lowering and event/non-Probe distinctions from the field.

The smallest wrong implementation joins by query, endpoint, or result type; accepts another runtime/occurrence; discards a port mode; turns non-Probe evidence into an event; or dispatches while verifying.

## Prohibitions at this boundary

- Do not introduce a compiler, general executor, resolver, dispatcher, scheduler, controller, table, or opcode.
- Do not create an event for a Pure, Generate, Check, or Warrant port merely because the pairing verifies.
- Do not erase exact source occurrence, port/mode, compiler, runtime-program, Probe event/route/decoder/resolution, or non-Probe typed-result/authority identities.

## Exit

On pass, update conformance and project state, then rederive the next strongest residual. On failure, preserve the actual return and reopen the earliest implicated field/lowering relation rather than broadening the architecture.
