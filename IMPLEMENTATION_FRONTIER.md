# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-REPRESENTATION-DEFECT-001` machine-checks a pair in one representation cell and distinct
declared horizon-relative classes. It keeps separator search/success, answer selection, solver,
execution, and successor meanings out of the boundary; Gate B remains pending.

## Strongest live obligation

Elaborate separating context question without treating a context predicate as an available,
successful, exhaustive, answer-selecting, executable, or successor-semantic separator.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-SEPARATING-CONTEXT-QUESTION
plan_phase: B
goal: Determine the smallest source-regenerative boundary for v2.0 separating-context questions, preserving a context family, pair of forms, consequence distinction predicate, context extension, availability, success, answer semantics, successor primitives, and Rust meaning as open unless independently established.
protected_difference: A separating-context question is a typed predicate over an admitted context family and a pair; it is distinct from an available or successful context, exhaustive search, answer, solver, program, or successor semantics.
discriminator: Contrast a source-bound context/consequence-difference question form with successful context selection, exhaustive failure, selected-answer, solver, program, semantic-evaluation, and omission claims; retain only the exact source-supported question boundary after regeneration and independent checking.
horizon: Separating-context-question boundary only; representation defect is fixed input, context-family membership, consequence meaning, extension availability, success/failure, answer semantics, execution, and all unrelated LegacyObligations remain explicit unless their exact source classification and independent checks establish otherwise, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014, D-9015, D-9016, D-9017, D-9018, D-9019, D-9020, D-9021
relevant_failures: F-9006, F-9007
if_pass: retain the smallest checked separating-context-question boundary and then elaborate the next residual selected by its unresolved relation
if_fail: reopen the earliest context family, pair, consequence, availability, success, answer, program, or source boundary relation; do not substitute proofs, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct a decisive source-bound context-predicate foil, then contrast it with available/successful
separator, exhaustive failure, selected-answer, solver, program, semantic-evaluation, and omission
claims. Contract only after exact source regeneration and retained obligations distinguish the
smallest context-question boundary.

## Prohibitions at this boundary

- Do not treat a separating context question as a successful separator, selected answer, total solver, program, or successor semantics.
- Do not use a source question form alone as an availability, exhaustion, answer-validity, or solver result.
- Do not use axioms, proof gaps, or Rust behavior to bypass schema typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest source-regenerative separating-context-question boundary, explicit
source obligations, and independent breakers, then select the next residual. On failure, preserve
the return and reopen the earliest context family, pair, consequence, availability, success, answer,
program, or source boundary relation.
