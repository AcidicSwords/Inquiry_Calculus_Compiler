# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-LLM-POSSIBILITY-RELATION-001` machine-checks the binding-level context/prompt/budget-to-
raw-completion relation, latent-field qualification, and weights/trace separation as source obligations. It keeps
latent-field behavior, direct access, execution, history construction, and successor meanings open; Gate B remains pending.

## Strongest live obligation

Elaborate the prompt-operator boundary while preserving rendering inputs, typed prompt output, occurrence membership, question/prompt non-identity, and contract-preserving wording variation without prompt execution or semantic identification.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-PROMPT-OPERATOR
plan_phase: B
goal: Determine the smallest source-regenerative boundary for the v2.0 prompt operator, preserving semantic question, boundary chart, active view, model parameters, policy, typed prompt, runtime occurrence membership, question/prompt distinction, and contract-preserving wording variation.
protected_difference: Prompt constraints are predecessor obligations; they are not prompt rendering execution, backend invocation, raw-completion interpretation, semantic-question identity, or semantic authority decision procedure.
discriminator: Contrast source-bound prompt/operator data with a foil that identifies question with prompt, treats changed wording as a changed contract, executes rendering or backend occurrence, or interprets raw completion; retain only the exact source-supported prompt boundary after regeneration and independent checking.
horizon: Prompt-operator boundary only; LLM possibility relation is fixed input, rendering behavior, backend execution, raw-completion interpretation, contract equivalence, semantic authority, and all unrelated LegacyObligations remain explicit unless independently formalized and checked, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014, D-9015, D-9016, D-9017, D-9018, D-9019, D-9020, D-9021
relevant_failures: F-9006, F-9007
if_pass: retain the smallest checked LLM possibility relation boundary and then elaborate the prompt operator
if_fail: reopen the earliest relation, latent-field, history-separation, access, execution, obligation, or source boundary relation; do not substitute proofs, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct a question-equals-prompt or wording-equals-contract-change foil, then contrast it with rendering inputs,
typed prompt output, occurrence membership, and contract-preserving wording variation. Contract only after exact
source regeneration and retained obligations distinguish the smallest prompt-operator boundary.

## Prohibitions at this boundary

- Do not execute prompt rendering or invoke the backend from this carrier.
- Do not identify the semantic question with prompt text or treat wording variation as contract change.
- Do not use axioms, proof gaps, or Rust behavior to bypass schema typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest prompt-operator boundary, explicit source obligations, and independent breakers, then
select the next residual. On failure, preserve the return and reopen the earliest input, rendering, prompt, occurrence,
identity, variation, execution, or source boundary relation.
