# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-BEHAVIORAL-COMPILER-CORRECTNESS-001` machine-checks the protected-continuation behavioral distinction,
exact/approximate compiler relation shapes, and approximation qualifier coordinates as source obligations. It keeps
behavior evaluation, correctness proof, and successor meanings open; Gate B remains pending.

## Strongest live obligation

Elaborate the question-conditioned LLM field while preserving active, prior, cross, retrieval, methods, and reopening projections without creating a separate memory ontology or implementing field construction.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-QUESTION-CONDITIONED-LLM-FIELD
plan_phase: B
goal: Determine the smallest source-regenerative boundary for the v2.0 question-conditioned LLM field, preserving its question/time index, six derived projections, and no-separate-memory-ontology constraint.
protected_difference: LLM-field constraints are predecessor obligations; they are not a memory ontology, field constructor, relevance decision, retrieval executor, method dispatcher, reopening evaluator, or semantic authority decision procedure.
discriminator: Contrast the six named question-conditioned projections with a foil that replaces them by a separate memory ontology or constructs/executes one of them; retain only the exact source-supported field boundary after regeneration and independent checking.
horizon: Question-conditioned-LLM-field boundary only; behavioral compiler correctness is fixed input, field/memory behavior, relevance, retrieval, method, reopening, semantic authority, and all unrelated LegacyObligations remain explicit unless independently formalized and checked, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014, D-9015, D-9016, D-9017, D-9018, D-9019, D-9020, D-9021
relevant_failures: F-9006, F-9007
if_pass: retain the smallest checked behavioral-compiler-correctness boundary and then elaborate the question-conditioned LLM field
if_fail: reopen the earliest continuation, behavior, equivalence, approximation, discriminator, error, reopening, proof, obligation, or source boundary relation; do not substitute proofs, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct a separate-memory-ontology or projection-executing foil, then contrast it with the declared active, prior,
cross, retrieval, methods, and reopening projections. Contract only after exact source regeneration and retained
obligations distinguish the smallest question-conditioned-field boundary.

## Prohibitions at this boundary

- Do not create a separate memory ontology or execute construction, relevance, retrieval, methods, or reopening in this carrier.
- Do not collapse the six projections or use a projection label as a semantic primitive or authority decision.
- Do not use axioms, proof gaps, or Rust behavior to bypass schema typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest question-conditioned-LLM-field boundary, explicit source obligations, and independent
breakers, then select the next residual. On failure, preserve the return and reopen the earliest question/time,
projection, ontology, construction, relevance, retrieval, method, reopening, or source boundary relation.
