# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-A-IMPLEMENTATION-CLASSIFICATION-001` establishes exact authority-separated roles for all
2,062 Rust semantic/public/schema identities and independently rejects omission, fuzzy edges,
blanket role collapse, promoted edge authority, lost provenance, and Gate A self-promotion. Its 29
direct/aggregate edge records are unproved ancestry, not correspondence standing or Gate A.

## Strongest live obligation

Classify the complete predecessor conformance field and connect executable evidence to the exact
claims and implementation surfaces it exercises without turning registration, a passing test, or a
status-row label into semantic or conformance warrant.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-A-FIXTURE-INVENTORY
plan_phase: A
goal: Review all 36 generated rust.conformance_module identities and all 166 conformance.fixture identities; preserve status-row and executable-registry roles separately, connect every executable fixture to its exact test function/module and every claimed protected behavior to reviewed TeX and implementation identities, and keep missing relations explicitly Unknown.
protected_difference: A typed fixture graph distinguishes source registration, executable occurrence, test implementation, predecessor status, exercised claim, and warranted coverage; merging those roles lets a green test or registry entry self-warrant conformance, while treating fixtures as unrelated files erases the executable pressure that must propagate into the successor.
discriminator: Generate a complete overlay from pinned test modules, status rows, and the executable registry; independently require all 202 source identities, exact test function existence and module/blob identity, exact fixture/status/registry incidence, valid reviewed TeX and implementation targets, and reject missing fixtures, fabricated claim edges, registry-only PASS promotion, copied evidence, blanket status, and Gate A self-promotion.
horizon: Phase A fixture/test classification at predecessor commit 4a18e2e only; TeX and implementation overlays remain fixed evidence, no test result proves successor semantics, three registry-only fixture identities remain explicit standing gaps, and no Rust mutation, Gate A promotion, or Phase B/F theorem is authorized
relevant_decisions: D-9001, D-9003, D-9007, D-9008, D-9009
relevant_failures: F-9005, F-9006, F-9007
if_pass: run FORMAL-A-COVERAGE-CHECKER over the joined TeX, implementation, and fixture overlays and determine Gate A only from that independent global return
if_fail: reopen the earliest fixture identity, registry/status incidence, test-function route, claim edge, evidence authority, or coverage relation; never invent standing for a registry-only fixture or delete it to make the join total
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Remove a whole test module, one fixture identity, or one registry edge; redirect a registry entry to
a nonexistent test or a foreign claim; then promote a registry-only fixture to PASS. Require the
checker to reject each at the exact identity/route/authority boundary while preserving the three
actual registry-only gaps as explicit Unknown standing relations.

## Prohibitions at this boundary

- Do not rewrite v2.0, infer semantic truth or coverage from a test occurrence, or select a primitive basis.
- Do not mark Gate A passed from overlay presence, test success, registry membership, or a green build.
- Do not advance the deferred v2 Rust frontier.
- Do not mix successor state into the predecessor ledgers.

## Exit

On pass, retain the fixture graph, exact execution routes, claim edges, standing gaps, and authority
breakers, then select `FORMAL-A-COVERAGE-CHECKER`. Gate A may pass only from that independent joined
coverage return. On failure, preserve the return and reopen the earliest fixture, route, claim,
standing, or coverage relation.
