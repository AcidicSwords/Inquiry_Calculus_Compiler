# Implementation Frontier

This file is the sole live implementation cursor. Historical work belongs in Git, demonstrated behavior in `CONFORMANCE_STATUS.md`, and accepted choices or failures in the JSONL ledgers.

Repository actuality must be probed before acting. The current residual was derived from demonstrated implementation behavior through predecessor coordinate `793d782`; that coordinate is ancestry, not a claim that a tracked document can name its own final commit or worktree state.

## Immediate demonstrated predecessor

`QRECIP-PROV-001` established that reciprocal return retains the complete admitted provenance chain. A foreign Probe-backed use can reach the same X exterior with a protected-different fiber, yet substituting it into the lawful reciprocal occurrence is rejected by exact use identity before endpoints can collapse the route. This adds no reciprocal opcode, controller, scheduler, canonical artifact, table, or second history.

## Strongest live obligation

Retain explicitly required discharge occurrences even when they are continuation-equivalent to or more expensive than another candidate; remove only optional strictly dominated questions.

<!-- LIVE_FRONTIER_BEGIN -->
id: QFRONTIER-REQDISCHARGE-001
plan_phase: 9
goal: Build a finite live-question frontier that retains every explicit Probe, Check, Warrant, support, or reconstruction discharge obligation independently of discretionary productivity and resource domination.
protected_difference: A required discharge remains live because authority demands its occurrence, even if all represented answers lead to the same protected continuation or a cheaper candidate dominates it. Generated productivity does not create required authority.
discriminator: Construct one required Check occurrence dominated by a cheaper continuation-equivalent optional question, one productive but unauthorized generated candidate, and one optional candidate strictly dominated by the required occurrence; retain the required occurrence, refuse to promote the generated candidate, and remove the dominated optional candidate.
horizon: one finite candidate set, explicit required/optional status, one finite resource preorder, one protected continuation partition, no global scheduling policy
relevant_decisions: D-0024, D-0097, D-0098, D-0127, D-0135, D-0139, D-0142
relevant_failures: F-0003
if_pass: QIFP-LOCAL-001 establish finite local closure only for a declared root/effectivity/coverage frontier with explicit residual exits
if_fail: reopen the smallest occurrence/authority/productivity/resource/continuation distinction; do not add a scheduler, controller, scalar policy, or hidden model choice
<!-- LIVE_FRONTIER_END -->

## Smallest decisive fixture

Start from a finite set of reified question occurrences with explicit discharge authority, protected continuation signatures, and resource-order relations. Include one required Check whose answers are continuation-equivalent, plus a cheaper optional candidate that dominates it on resources.

Add a productive generated candidate with no required authority, and an optional candidate strictly dominated by the required Check. The frontier must retain the required Check, keep the generated candidate optional/unpromoted, and remove the dominated optional candidate. Preserve incomparable candidates rather than inventing a scalar score.

The smallest wrong implementation removes the required occurrence because it is continuation-equivalent or resource-dominated, or promotes a generated candidate to required merely because it is productive.

## Prohibitions at this boundary

- Do not collapse required discharge into discretionary answer-productivity.
- Do not promote Generate output to Probe, Check, Warrant, support, or reconstruction authority.
- Do not scalarize incomparable resource/risk orders or synthesize a scheduler, controller, global policy, authoritative table, or second history.

## Exit

On pass, update conformance and any warranted append-only decision, replace the live block with `QIFP-LOCAL-001`, and continue. On failure, preserve the actual return and reopen the earliest implicated occurrence/authority/productivity/resource/continuation relation rather than broadening the architecture.
