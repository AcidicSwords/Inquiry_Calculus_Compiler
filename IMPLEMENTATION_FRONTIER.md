# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-RELATION-EXPRESSION-IR-001` regenerates three exact v2.0 source identities into a typed
candidate data-only relation-expression grammar while preserving every source status as Ambiguous.
The independent checker rejects thirteen source, status, grammar, denotation, semantic-question,
and promotion mutations; Gate B remains pending.

## Strongest live obligation

Elaborate the v2.0 relation-schema and named-port boundary without identifying typed ports with
raw strings, declaring schema instances as semantic questions, or consuming later refinement,
question, probe, or program semantics.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-RELATION-SCHEMAS-PORTS
plan_phase: B
goal: Classify and reconstruct the v2.0 typed relation-schema and named-port boundary against checked relation and IR syntax, retaining its exact formal/obligation source split and postponing schema denotation, question semantics, refinement, probe, program, successor-primitive, and Rust meaning.
protected_difference: Named ports must remain typed relation-schema coordinates rather than strings or unordered raw fields. A schema description is neither an instantiated relation nor a concrete semantic question; any refinement or question-use claims belong to later layers.
discriminator: Construct an overcomplete typed schema/port carrier from every exact source definition, contrast it with string labels, untyped records, schema-as-instance, semantic-question promotion, and omission, then contract only after exact source regeneration and retained obligations isolate the smallest responsible representation.
horizon: Relation schemas and named ports only; typed relations and candidate IR are fixed inputs, schema denotation, relation instances, refinement, questions, probes, programs, standing, and all unrelated LegacyObligations remain explicit, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014, D-9015, D-9016, D-9017, D-9018, D-9019
relevant_failures: F-9006, F-9007
if_pass: retain the typed schema/port representation and source obligations, then elaborate the next residual selected by the relation-schema boundary
if_fail: reopen the earliest schema source, port typing, source-boundary, or semantic-question contamination relation; do not substitute strings, untyped fields, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct every exact relation-schema and named-port alternative as an overcomplete typed carrier,
then contrast it with string labels, untyped records, schema instances, semantic questions, and
omission. Contract only after exact source regeneration and retained obligations distinguish the
smallest representable schema surface.

## Prohibitions at this boundary

- Do not treat raw strings or untyped fields as named ports.
- Do not treat a schema as an instance, semantic question, or theorem authority.
- Do not use axioms, proof gaps, or Rust behavior to bypass schema typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest source-regenerative schema/port carrier, explicit source obligations,
and independent breakers, then select the next residual. On failure, preserve the return and reopen
the earliest schema, port, source, or semantic-question boundary relation.
