# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-FORMS-001` regenerates seven exact v2.0 source identities into a binding-indexed
dependent represented-form carrier, typed reification and partial operational interpretation
boundaries, and three retained form obligations. The independent checker rejects nine source,
representation, totalization, semantics-leakage, and promotion mutations; Gate B remains pending.

## Strongest live obligation

Elaborate the explicit v2.0 typed-relation surface against the checked binding/type and form layers
without reducing relations to host functions, asserting unadmitted relational laws, or consuming
the later formula, question, fiber, or program layers.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-TYPED-RELATIONS
plan_phase: B
goal: Reconstruct the explicit v2.0 typed binary and n-ary relation surface against binding-indexed admitted types, retaining the exact formal/obligation source split and postponing formula, question, fiber, program, successor-primitive, and Rust meaning.
protected_difference: A typed relation relates admitted predecessor type realizations without being silently narrowed to a total host function; raw predicates, function coercion, or laws imported before their exact source layer erase relation direction, partiality, arity, or representation conditions.
discriminator: Construct an overcomplete binding-indexed relation carrier from the explicit typed-relation definitions, contrast it with raw host predicates, total functions, and relation objects that embed formula/question/program semantics, then contract only after exact source regeneration, typed endpoints, and retained source obligations isolate the smallest responsible surface.
horizon: Typed relation forms only; binding/type and represented-form artifacts are fixed inputs, formulas, questions, fibers, programs, standing, classicality, and all unrelated LegacyObligations remain explicit, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014
relevant_failures: F-9006, F-9007
if_pass: retain the typed relation carrier and exact source obligations, then elaborate the next typed relation-expression or formula boundary selected by the residual
if_fail: reopen the earliest typed-relation identity, endpoint attachment, source-boundary, or semantics-contamination relation; do not substitute raw predicates, total-function assumptions, unchecked casts, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct every explicit typed-relation alternative as an overcomplete binding-indexed carrier,
then contrast it with raw host predicates, total functions, and relations that already embed later
formula/question/program semantics. Contract only after exact source regeneration and typed endpoint
preservation distinguish which data belongs to the relation layer rather than later layers.

## Prohibitions at this boundary

- Do not treat raw host predicates or total functions as the typed relation surface.
- Do not place formula, question, fiber, or program semantics in the relation carrier.
- Do not use strings, unchecked casts, axioms, proof gaps, or Rust types to bypass typed endpoints.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest source-regenerative typed-relation carrier, endpoint boundary, explicit
unresolved obligations, and independent breakers, then select the next residual. On failure,
preserve the return and reopen the earliest relation, binding/type, source, or semantics-boundary relation.
