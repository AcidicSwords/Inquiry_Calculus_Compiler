# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-A-TEX-CLASSIFICATION-001` establishes an exact, regenerable construction-specification
disposition for all 1,370 generated TeX candidates and independently rejects omission, keyword-only
selection, blanket theorem promotion, lost source roles, and Gate A self-promotion. It establishes
TeX source classification only, not Phase B elaboration, a successor primitive, or Gate A.

## Strongest live obligation

Classify predecessor Rust semantic modules, public Rust items, and schemas without treating
implementation occurrence as semantic authority, and connect each consequential implementation
surface to an exact classified TeX identity or an explicit `ImplementationOnly` disposition.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-A-RUST-SURFACE-INVENTORY
plan_phase: A
goal: Review every generated rust.semantic_module_candidate, rust.public_item, schema.fixture, and schema.migration identity; classify its implementation role, link every claimed semantic correspondence to one or more exact reviewed TeX identities, and mark surfaces with no semantic claim as ImplementationOnly without rewriting either predecessor source.
protected_difference: Explicit source-to-claim edges reveal whether Rust preserves, approximates, extends, or merely implements classified predecessor material; inferring semantics from public code promotes implementation accidents, while marking all code ImplementationOnly erases actual correspondence and future propagation obligations.
discriminator: Maintain a complete overlay for 64 semantic modules, 1,989 public Rust items, and 9 schemas; independently require exact inventory identity and blob/range digests, validate every semantic edge against the reviewed TeX overlay, reject orphan or fabricated claim edges and blanket classifications, and keep conformance test modules for the separate fixture residual.
horizon: Phase A Rust semantic/public/schema classification at predecessor commit 4a18e2e only; 36 Rust conformance modules and 166 fixture identities remain in FORMAL-A-FIXTURE-INVENTORY, TeX classifications remain fixed evidence, and no Rust mutation, semantic repair, Gate A promotion, or successor correspondence theorem is authorized
relevant_decisions: D-9001, D-9003, D-9007, D-9008
relevant_failures: F-9005, F-9006
if_pass: select FORMAL-A-FIXTURE-INVENTORY while retaining exact implementation-to-claim edges and authority-separation breakers
if_fail: reopen the earliest inventory identity, implementation-role, claim-edge, schema, authority, or coverage relation; never infer a TeX claim from code shape or delete an inconvenient implementation identity
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Remove a whole semantic module, one public item, or one schema; attach a public API to a foreign or
missing TeX identity; then classify the remaining field once as semantic and once as
`ImplementationOnly`. Require exact rejection of omission, fabricated authority, and both blanket
collapses before contracting the implementation field.

## Prohibitions at this boundary

- Do not rewrite v2.0, infer TeX or successor truth from Rust, or select a primitive basis.
- Do not mark Gate A passed from TeX closure, implementation-overlay presence, or a green build.
- Do not advance the deferred v2 Rust frontier.
- Do not mix successor state into the predecessor ledgers.

## Exit

On pass, retain the implementation-role overlay, its exact TeX edges, and authority breakers, then
select `FORMAL-A-FIXTURE-INVENTORY`. Gate A passes only after fixture-to-claim edges and the full
cross-surface breaker field close. On failure, preserve the return and reopen the earliest identity,
role, edge, or authority relation.
