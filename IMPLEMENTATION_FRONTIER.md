# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-A-EXTRACTION-001` establishes a byte-pinned, regenerable source universe and independent
omission breakers across the v2.0 TeX, predecessor repository surfaces, public Rust visibility
lines, schemas, conformance rows, and executable fixture registry. It establishes extraction and
identity only, not reviewed semantic classification, a successor primitive, or Gate A.

## Strongest live obligation

Classify the generated TeX candidate field into exact mathematical declarations, normative claims,
expository/context-only material, and unresolved claim boundaries, assigning every consequential
item a reviewed formalization destination or explicit disposition.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-A-TEX-INVENTORY
plan_phase: A
goal: Review every generated tex.declaration, tex.narrative, tex.list_item, and tex.display candidate against its exact source range; record whether it is a formalizable predecessor claim, a claim boundary needing decomposition, expository/context-only material, or an unresolved conflict, with an exact Phase B destination where applicable.
protected_difference: Reviewed item-level classification preserves the actual normative and mathematical predecessor surface, while keyword classification or treating every paragraph/display as one claim either drops claims without trigger words or promotes exposition and fragments into false semantic obligations.
discriminator: Maintain a complete classification overlay keyed by generated stable identities; independently require every TeX candidate exactly once, reject unknown/missing/duplicate identities and invalid dispositions, and use strong section-level omission and claim-boundary breakers before contracting candidates from pending review.
horizon: Phase A TeX candidate classification over the pinned 203 declarations, 516 narratives, 426 list items, and 225 displays only; generated source coverage remains fixed, Rust/schema/fixture classification remains residual, and no semantic repair, primitive selection, Gate A promotion, or Rust change is authorized
relevant_decisions: D-9001, D-9003, D-9004, D-9005, D-9006, D-9007
relevant_failures: F-9002, F-9005
if_pass: select the strongest remaining Rust/schema/fixture classification residual while retaining exact TeX classification and source-coverage breakers
if_fail: reopen the earliest candidate extraction, claim-boundary, authority, destination, or disposition relation; never delete the source identity to make classification total
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Remove a whole TeX section's classification entries, classify all candidates by normative keyword
alone, or merge a declaration with adjacent exposition; require the overlay checker to identify the
exact missing identities, unsupported disposition, or lost claim boundary. The smallest wrong
implementation is marking every generated candidate formalize/expository without source review.

## Prohibitions at this boundary

- Do not rewrite v2.0, infer successor truth from Rust, or select a primitive basis.
- Do not mark Gate A passed from source coverage, classification-file presence, or a green Lean build.
- Do not advance the deferred v2 Rust frontier.
- Do not mix successor state into the predecessor ledgers.

## Exit

On pass, retain the classification overlay and its section-level breakers, then select the strongest
remaining Rust/schema/fixture classification residual. Gate A passes only after all Phase A source
classes and cross-edges close together. On failure, preserve the return and reopen the earliest
candidate or classification relation.
