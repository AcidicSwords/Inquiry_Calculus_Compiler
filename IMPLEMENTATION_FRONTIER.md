# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-SOURCE-SAFE-ASK-LOWERING-001` machine-checks typed port evidence, probe identity and
route provenance, bundle/resolver structure, and the source-continuation gate. It keeps event
actuality, resolver computation, compilation, execution, solver, and successor meanings open; Gate B remains pending.

## Strongest live obligation

Elaborate the unproved source-Ask-compilation safety claim while preserving its explicit dependence on source-safe lowering and its direct-runtime-Probe counterposition.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-SOURCE-ASK-COMPILATION-SAFETY-CLAIM
plan_phase: B
goal: Determine the smallest source-regenerative carrier for the unproved v2.0 source-Ask-compilation safety law, preserving its source-safe-lowering dependency and direct-runtime-Probe nonimplication.
protected_difference: The predecessor law is an unproved claim; a direct runtime raw Probe is not thereby a lawful lowering of an occurrence-specific source Ask.
discriminator: Contrast the source law's declared dependency with a direct runtime raw-Probe foil; retain the exact claim premise and counterposition without claiming compiler correctness, source safety, or semantic execution after regeneration and independent checking.
horizon: Source-Ask-compilation-safety claim only; source-safe lowering is fixed input, compiler behavior, source semantics, event actuality, resolver computation, execution, answer semantics, and all unrelated LegacyObligations remain explicit unless independently formalized and checked, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014, D-9015, D-9016, D-9017, D-9018, D-9019, D-9020, D-9021
relevant_failures: F-9006, F-9007
if_pass: retain the smallest checked source-safe Ask-lowering boundary and then elaborate the unproved source-Ask-compilation safety claim
if_fail: reopen the earliest source configuration, occurrence, port, event, raw, route, resolver, continuation, obligation, or source boundary relation; do not substitute proofs, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct a direct runtime raw-Probe foil, then contrast it with an occurrence-specific source Ask and its retained
source-safe-lowering dependency. Contract only after exact source regeneration and retained obligations distinguish the
smallest unproved source-Ask-compilation safety-claim boundary.

## Prohibitions at this boundary

- Do not prove, axiomatically assume, or otherwise promote the source-Ask-compilation safety law.
- Do not treat a direct runtime raw Probe as a lawful source-Ask lowering.
- Do not use axioms, proof gaps, or Rust behavior to bypass schema typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest source-Ask-compilation safety-claim boundary, explicit source obligations, and independent
breakers, then select the next residual. On failure, preserve the return and reopen the earliest law, lowering,
occurrence, raw-Probe, compiler, or source boundary relation.
