# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-QUESTION-JOINT-ACTIVE-REFINEMENT-001` machine-checks the local paired-profile equality
laws behind the v2.0 joint and active kernel intersections. The independent checker rejects eleven
source, promotion, carrier, pairing, global-kernel, program, and axiom mutations; Gate B remains pending.

## Strongest live obligation

Elaborate question redundancy without treating a factorization witness or kernel inclusion as an
unconditional global active-representation theorem.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-QUESTION-REDUNDANCY-BOUNDARY
plan_phase: B
goal: Determine the smallest source-regenerative boundary for the v2.0 question-redundancy proposition, preserving its factorization/kernel direction conditions, all global representation, successor-primitive, and Rust meaning as open unless independently established.
protected_difference: Redundancy is a relation between one question profile and a current representation; it is neither automatic from joint pairing nor an unconditional representation-improvement or global semantic claim.
discriminator: Contrast a source-bound redundancy candidate relation with an unconditioned factorization/kernel equivalence, representation-improvement promise, program, and omission; retain only the exact source-supported boundary after regeneration and independent checking.
horizon: Question-redundancy boundary only; local preorder, conditional factorization/kernel syntax, and joint/active paired-profile equality are fixed inputs, reverse factorization without coverage, global active-representation semantics, improvement/cost ordering, actual execution, and all unrelated LegacyObligations remain explicit unless their exact source classification and independent checks establish otherwise, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014, D-9015, D-9016, D-9017, D-9018, D-9019, D-9020, D-9021
relevant_failures: F-9006, F-9007
if_pass: retain the smallest checked question-redundancy boundary and then elaborate the next residual selected by its unresolved relation
if_fail: reopen the earliest redundancy source, factorization, kernel, representation, or semantics/program contamination relation; do not substitute proofs, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct a decisive source-bound redundancy foil, then contrast it with unconditioned
factorization/kernel, representation-improvement, program, and omission claims. Contract only after
exact source regeneration and retained obligations distinguish the smallest redundancy surface.

## Prohibitions at this boundary

- Do not treat a redundancy coordinate as a proved global kernel, representation, or improvement law.
- Do not use a source proposition alone as an unconditioned reverse factorization proof.
- Do not use axioms, proof gaps, or Rust behavior to bypass schema typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest source-regenerative redundancy boundary, explicit source
obligations, and independent breakers, then select the next residual. On failure, preserve the
return and reopen the earliest redundancy, factorization, kernel, representation, program, or source boundary relation.
