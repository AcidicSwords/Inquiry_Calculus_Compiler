# Implementation Frontier

This file is the sole live implementation cursor. Historical work belongs in Git, demonstrated behavior in `CONFORMANCE_STATUS.md`, and accepted choices or failures in the JSONL ledgers.

Repository actuality must be probed before acting. The current residual was derived from demonstrated implementation behavior through predecessor coordinate `da87775`; that coordinate is ancestry, not a claim that a tracked document can name its own final commit or worktree state.

## Immediate demonstrated predecessor

`QASK-COLD-REGEN-002` established that the derived nonexecuting source-Ask lowering regenerates from ordinary file-backed source/configuration/occurrence/runtime roots after restart. It rechecks a fresh catalog, retains exact port/mode/compiler/runtime coordinates, rejects substituted occurrence/runtime identities, and makes no provider call. The runtime-program artifact now explicitly enumerates its direct immutable dependencies for strict store insertion. No lowering recipe, compiler, resolver, event, table, or opcode was added.

## Strongest live obligation

Determine the smallest execution-conditioned bridge from a checked source Ask lowering to the existing port-indexed discharge bundle, preserving distinct port authority without introducing a general compiler or dispatch mechanism.

<!-- LIVE_FRONTIER_BEGIN -->
id: QASK-DISCHARGE-BUNDLE-003
plan_phase: 5
goal: Derive the narrow bridge that admits an existing checked port-indexed finite discharge bundle only for the exact source Ask lowering it discharges, preserving every source port/mode and event/route authority already carried by the bundle.
protected_difference: A checked source lowering and a checked bundle can each be valid while naming different Ask occurrences, port sets, modes, compiler coordinates, or event/route evidence. Pairing them by compatible query, endpoint, or runtime target would erase execution authority.
discriminator: Construct one finite all-Probe source Ask lowering and one existing all-port bundle. Accept only when the exact occurrence, compiler, and complete port/mode field agree; reject a bundle from a distinct occurrence, omitted/foreign/mode-changed port evidence, or a lowering/runtime substitution. Verification must recheck existing evidence without dispatching.
horizon: one finite all-Probe source Ask, one existing finite discharge bundle, exact occurrence/compiler/port/mode and existing bundle provenance, and no dispatch; no new execution bundle schema, compiler, resolver procedure artifact, general non-Probe executor, cross-binding bridge, scheduler, controller, table, or opcode
relevant_decisions: D-0125, D-0150, D-0154, D-0156, D-0157
relevant_failures: F-0001, F-0002
if_pass: rederive the strongest remaining Phase 5/6 residual from the stable plan and pending conformance; do not reopen passed QASK/QSUCC/QREADY/QACTUAL fixtures without a new breaker
if_fail: reopen the earliest source-lowering/bundle occurrence, compiler, port/mode, or event/route identity relation; do not build a general compiler, dispatcher, scheduler, controller, table, or opcode
<!-- LIVE_FRONTIER_END -->

## Smallest decisive fixture

Pair one checked all-Probe `SourceAskLowering` with the existing checked finite discharge bundle. Rewalk both sides and compare their exact source occurrence, compiler, and complete port/mode fields; preserve the bundle's own event/route/decoder/resolution provenance rather than duplicating it.

The smallest wrong implementation joins lowering and bundle by question, endpoint, or runtime target alone; treats all ports as one event; discards a port mode; replaces bundle provenance; or dispatches while verifying.

## Prohibitions at this boundary

- Do not introduce a second execution bundle or generalize the bridge into a compiler, resolver, dispatcher, scheduler, controller, table, or opcode.
- Do not execute or create an event merely because the bridge verifies.
- Do not erase exact source occurrence, port/mode, compiler, runtime-program, event, route, decoder, or resolution identities.

## Exit

On pass, update conformance and project state, then rederive the next strongest residual. On failure, preserve the actual return and reopen the earliest implicated lowering/bundle relation rather than broadening the architecture.
