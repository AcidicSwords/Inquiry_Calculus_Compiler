# Implementation Frontier

This file is the sole live implementation cursor. Historical work belongs in Git, demonstrated behavior in `CONFORMANCE_STATUS.md`, and accepted choices or failures in the JSONL ledgers.

Repository actuality must be probed before acting. The current residual was derived from demonstrated implementation behavior through predecessor coordinate `588250c`; that coordinate is ancestry, not a claim that a tracked document can name its own final commit or worktree state.

## Immediate demonstrated predecessor

`QADJOINT-001` established under exact finite coverage that a binding-supplied forward/backward pair is admitted only after rechecking both typed preorders, both total maps, and every pointwise adjunction-law pair. A structurally valid backward foil is rejected with its exact law counterexample. This adds no adjoint opcode, controller, scheduler, canonical artifact, authoritative table, or second history.

## Strongest live obligation

Reject a generic backward route as reciprocal return when it does not retain the exact exterior-producing negation-use and same-use fiber provenance.

<!-- LIVE_FRONTIER_BEGIN -->
id: QRECIP-PROV-001
plan_phase: 10
goal: Admit reciprocal return only through the exact oriented negation-use that produced the exterior, its admitted departure and coverage, and the reverse fiber of that same use.
protected_difference: Equal endpoints or a generic backward/converse route do not preserve reciprocal provenance. Same-use return is indexed by the exterior-producing use occurrence, not merely by source and result forms.
discriminator: Construct two oriented uses that reach the same exterior with protected-different reverse fibers; build the lawful same-use return and a swapped/generic backward foil, then admit only the former while retaining the mismatched use identities.
horizon: one finite distinction, two admitted oriented uses, one shared exterior, two use-tagged reverse fibers, no universal reciprocal theorem
relevant_decisions: D-0024, D-0097, D-0098, D-0127, D-0135, D-0140, D-0141
relevant_failures: F-0003
if_pass: QFRONTIER-REQDISCHARGE-001 retain required discharge occurrences despite continuation equivalence or resource domination
if_fail: reopen the smallest use/departure/coverage/exterior/fiber/provenance distinction; do not add a reciprocal opcode, controller, scheduler, table, or second history
<!-- LIVE_FRONTIER_END -->

## Smallest decisive fixture

Start from two separately admitted oriented negation uses whose incidences reach the same exterior but whose reverse sections contain protected-different sources. Preserve each use identity, admitted departure, semantic/execution coverage, tagged exterior occurrence, and whole reverse fiber.

Construct the lawful return through the exact use that produced its exterior. Then substitute the other use's fiber or an otherwise endpoint-compatible generic backward route. The reciprocal occurrence checker must reject the substitution and expose the mismatched use identities. The fixture must reuse ordinary relation-use and fiber machinery and add no controller or history.

The smallest wrong implementation keys reciprocal return by endpoints and accepts a backward fiber from another use occurrence.

## Prohibitions at this boundary

- Do not treat converse orientation, generic backward traversal, common endpoints, or equal selected returns as same-use reciprocal return.
- Do not erase negation-use, departure, semantic/execution coverage, exterior-occurrence, or whole-fiber provenance.
- Do not synthesize a reciprocal opcode, controller, scheduler, global policy, table, or second history to make the local case pass.

## Exit

On pass, update conformance and any warranted append-only decision, replace the live block with `QFRONTIER-REQDISCHARGE-001`, and continue. On failure, preserve the actual return and reopen the earliest implicated use/departure/coverage/exterior/fiber/provenance relation rather than broadening the architecture.
