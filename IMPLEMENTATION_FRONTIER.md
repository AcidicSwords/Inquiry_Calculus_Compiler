# Formal Successor Frontier

This is the single live autonomous cursor on `codex/formal-successor`. The predecessor Rust cursor
at branch point `4a18e2e308f359a64f19b7d056652f19fd9aaeae` is deferred and recorded in
`formal-successor/PREDECESSOR_BASELINE.md`; Git retains its exact content.

## Immediate demonstrated predecessor

`FORMAL-B-RESOLUTION-OUTCOME-001` machine-checks five resolution outcomes, supported-only
answer-slot eligibility, and separate operational stop states. It keeps certificate validity,
answer filling, execution, provider state, solver, and successor meanings out of the boundary; Gate
B remains pending.

## Strongest live obligation

Elaborate the inquiry source grammar while preserving first-order expressions, inspectable continuation data, and capture-safe answer binding.

<!-- LIVE_FRONTIER_BEGIN -->
id: FORMAL-B-INQUIRY-SOURCE-GRAMMAR
plan_phase: B
goal: Determine the smallest source-regenerative boundary for v2.0 inquiry source grammar, preserving Return/Ask program syntax, answer slots, explicit environments, and capture-safe substitution.
protected_difference: An Ask continuation is inspectable serializable program data with a typed answer slot, not a host-language callback or hidden model policy; returned distinctions may construct new questions.
discriminator: Contrast source-bound first-order grammar and explicit continuation package with opaque closures, hidden model policy, unchecked substitution, fixed-checklist restriction, answer selection, solver, and semantic-evaluation claims; retain only the exact source-supported grammar boundary after regeneration and independent checking.
horizon: Inquiry-source-grammar boundary only; resolution outcome is fixed input, source typing, execution, substitution implementation, answer semantics, program interpretation, and all unrelated LegacyObligations remain explicit unless their exact source classification and independent checks establish otherwise, no Phase C primitive decision or Phase F Rust correspondence is authorized, and Gate B stays pending
relevant_decisions: D-9003, D-9008, D-9012, D-9013, D-9014, D-9015, D-9016, D-9017, D-9018, D-9019, D-9020, D-9021
relevant_failures: F-9006, F-9007
if_pass: retain the smallest checked inquiry-source-grammar boundary and then elaborate the next residual selected by its unresolved relation
if_fail: reopen the earliest expression, continuation, substitution, answer-slot, or source boundary relation; do not substitute proofs, axioms, or Rust behavior
<!-- LIVE_FRONTIER_END -->

## Smallest decisive breaker

Construct a decisive source-bound opaque-closure and hidden-model-policy foil, then contrast it with first-order
Return/Ask syntax, answer slots, explicit environment, inspectable serializable continuation data, capture-safe
substitution, generated-question construction, selected-answer, solver, and omission claims. Contract only after
exact source regeneration and retained obligations distinguish the smallest inquiry-source-grammar boundary.

## Prohibitions at this boundary

- Do not model Ask as an opaque host callback or hidden model policy, and do not leave substitution capture-unsafe.
- Do not treat this carrier as answer selection, a solver, executable interpretation, or successor semantics.
- Do not use axioms, proof gaps, or Rust behavior to bypass schema typing.
- Do not select successor primitives or mark Gate B passed from this local elaboration.
- Do not rewrite v2.0, advance the deferred Rust frontier, or mix successor state into predecessor ledgers.

## Exit

On pass, retain the smallest inquiry-source-grammar boundary, explicit source obligations, and independent
breakers, then select the next residual. On failure, preserve the return and reopen the earliest expression,
continuation, substitution, answer-slot, or source boundary relation.
