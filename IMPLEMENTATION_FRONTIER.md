# Implementation Frontier

This file is the sole live implementation cursor. Historical work belongs in Git, demonstrated behavior in `CONFORMANCE_STATUS.md`, and accepted choices or failures in the JSONL ledgers.

Repository actuality must be probed before acting. The current residual was derived from demonstrated implementation behavior through predecessor coordinate `bb584b6`; that coordinate is ancestry, not a claim that a tracked document can name its own final commit or worktree state.

## Immediate demonstrated predecessor

`QASK-MIXED-LOWERING-005` established a derived pairing between the finite mixed field and an exact checked source/runtime lowering. It rechecks both operands and their complete port/mode map, retaining the source occurrence/compiler/runtime coordinate while leaving actual Probe and non-Probe evidence in the mixed field. No compiler, executor, resolver, table, or opcode was added.

## Strongest live obligation

Demonstrate cold regeneration of the combined mixed discharge field and source/runtime lowering after file-backed restart, using ordinary persisted roots and no provider redispatch.

<!-- LIVE_FRONTIER_BEGIN -->
id: QASK-MIXED-COLD-REGEN-006
plan_phase: 5
goal: Regenerate one exact mixed field/lowering pair from ordinary persisted source, runtime, event, resolution, typed-form, and authority roots after store reopen.
protected_difference: A warm mixed field can retain source/event/type values that a fresh replay cannot reconstruct. Persisting the derived field would conceal a missing ordinary root and turn a view into authority.
discriminator: Persist the mixed source/configuration/occurrence/runtime, actual event, resolution path, typed Generate result, and authority references; close/reopen, rebuild a fresh catalog, decode/recheck roots, regenerate both views, and reject a substituted occurrence/runtime or non-Probe authority with zero provider calls.
horizon: one file-backed mixed Probe/Generate Ask, one runtime artifact, one source-linked actual event, and one typed non-Probe result; no persisted derived view, compiler procedure, general executor, resolver procedure artifact, event for non-Probe modes, cross-binding bridge, scheduler, controller, table, or opcode
relevant_decisions: D-0125, D-0156, D-0157, D-0158, D-0159, D-0160
relevant_failures: F-0001, F-0002
if_pass: rederive the strongest remaining Phase 5/6 residual from the stable plan and pending conformance; do not reopen passed QASK/QSUCC/QREADY/QACTUAL fixtures without a new breaker
if_fail: reopen the earliest persisted root, fresh catalog reconstruction, mixed field/lowering occurrence, port/mode, compiler, runtime, typed-result, or authority-route relation; do not persist the derived views or build a general compiler, executor, dispatcher, scheduler, controller, table, or opcode
<!-- LIVE_FRONTIER_END -->

## Smallest decisive fixture

Persist ordinary roots for one mixed field/lowering pair, reopen the store, rebuild a fresh catalog, then regenerate and recheck both derived views. The reopened proof must retain exact source occurrence, port/mode, compiler/runtime, Probe event/route/resolution, and non-Probe typed-result/authority coordinates with no provider call.

The smallest wrong implementation reuses a warm field or lowering, persists the derived view, loads only compatible projections, accepts another runtime/occurrence, discards an authority coordinate, turns non-Probe evidence into an event, or dispatches while regenerating.

## Prohibitions at this boundary

- Do not persist either derived view or introduce a compiler, general executor, resolver, dispatcher, scheduler, controller, table, or opcode.
- Do not create an event for a Pure, Generate, Check, or Warrant port merely because the pairing regenerates.
- Do not erase exact source occurrence, port/mode, compiler, runtime-program, Probe event/route/decoder/resolution, or non-Probe typed-result/authority identities.

## Exit

On pass, update conformance and project state, then rederive the next strongest residual. On failure, preserve the actual return and reopen the earliest implicated persisted-root or field/lowering relation rather than broadening the architecture.
