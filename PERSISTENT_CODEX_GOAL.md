# PERSISTENT_CODEX_GOAL.md

## Status

**Standing autonomous implementation goal.**

This file does not define Inquiry Calculus semantics. Semantic authority remains:

`Inquiry_Calculus_Unified_Canonical_Specification_v1_1.tex`

The implementation-facing architecture/order remains:

`Inquiry_Calculus_v1_1_Comprehensive_Implementation_Plan.md`

The repository-wide execution discipline remains:

`AGENTS.md`

Research-derived context and later-phase deltas are explained in:

`PROJECT_RESEARCH_IMPLEMENTATION_HANDOFF.md`

That handoff is the final self-contained checkpoint of the research crawl closed for the 25 August 2026 integration. Its supporting corpus is present under `research/final-2026-08-25/`. Use it only as derived breaker, compression, and phase-local guidance; it is not a second implementation plan and cannot silently amend semantics.

When a narrower user instruction is active, satisfy it first. Unless that instruction changes or revokes this standing goal, resume this program afterward.

---

# 1. Persistent objective

Autonomously advance the repository from its **current demonstrated implementation state** to the smallest complete, executable, cold-replayable reference implementation of the current canonical Inquiry Calculus.

The project is not complete when:

- a local patch passes;
- all current tests happen to pass;
- a phase has record types but no real execution path;
- the agent can describe the intended runtime;
- a generated candidate is structurally coherent but not independently discharged.

The standing target is the complete successor chain:

```text
typed forms / relations
-> partially bound question
-> source IProg
-> actual/pure discharge by declared authority
-> preserved raw return
-> decoding / resolution
-> checked and independently warranted standing
-> supported live determination presentation
-> positive, relevant, non-circular departure
-> admitted coverage-indexed NegationUse
-> tagged positive exterior
-> same-use reverse return fiber
-> protected recovery
-> seed / reorientation
-> reciprocal second side
-> residuals and Gamma post-check
-> generic separator inquiry
-> representation / probe / binding extension where required
-> paired actual history and replay
-> active/reserve/recovery views
-> native method invocation through typed contracts
-> residual-driven method continuation
-> regenerative fold / recovery / reopening
-> conservative cross-binding standing transport
-> predecessor-judged self-revision
-> complete cold replay
```

Continue from the strongest current executable residual until this target is demonstrated or a lawful stop is reached.

---

# 2. Current implementation coordinate

This goal has one moving coordinate: the current clean `main` head after its required gates and
both CI jobs pass. Never encode an old commit as the live starting point. Reconstruct the exact
head from Git, then read `IMPLEMENTATION_FRONTIER.md` for the single current obligation.

The post-research baseline at `b761369` is historical ancestry. Since that baseline, the following
ratchets have been demonstrated and pushed:

```text
RPL-001 completed-effect reload
RPL-002--005 finite decode, support, source binding, lowering regeneration, and failure separation
CYCLE-001 complete Ask -> actual return -> admitted resumption -> Return with zero-redispatch cold replay
TRACE-001 paired question/return actuality with path-sensitive provenance
PRESENT-001 exact finite sufficient-present factorization and positive reopening witness
```

The current Phase 10 ratchet is a real-provider vertical slice. The active provider is the locally
installed Ollama runtime with `qwen3.5:9b`; its live schema-constrained probe succeeded through the
existing durable `ProbeProvider` boundary. Exact HTTP status/body bytes and the ordinary event were
committed before decoding two distinct candidates. Deterministic tests preserve success and
non-success actuality and keep framing, transport, completion, and candidate failures distinct.
The OpenAI adapter remains a secondary optional route; `F-0001` records only its credential
constraint and does not block the local implementation path.

The OpenAI heterogeneous-output decoder and Ollama schema-response decoder both preserve all
candidates without claiming support. The positive identity-cycle
breaker was also resolved: observation support may be formed after the raw return while every other
occurrence-context field remains fixed, and exact standing/raw-return admission still applies.
Each decoded string now has a provider-specific minimal content-addressed value identity over raw
return, decoder version, ordinal, and exact text. Both OpenAI and Ollama value identities regenerate
after a file-backed restart; the active typed-completion chain uses the Ollama values.

The local provider/replay chain is now complete at its declared finite scope:

```text
fresh successful local-model return and decoder version
-> post-actuality typed forms and completion candidates over every regenerated decoded value
-> independently supported answer admission using post-return environments
-> capture-safe continuation binding and runtime resumption
-> the same protected result after restart with zero redispatch
```

`PROVIDER-001D` demonstrates the deterministic post-actuality composition, and `PROVIDER-001E`
now runs that whole sequence against the installed model itself. The next obligation is `TRACE-002`:
use a finite multi-event traversal to extend paired actuality and sufficient-present/reopening while
keeping ordinary history authoritative and all traces/presents derived.

`TRACE-002A` is now established as a narrow prerequisite: derived traversal membership is exactly
ledger-covered, while causal order is explicitly `Unknown` or separately declared and acyclic. It
does not infer a causal edge from ledger order. The unresolved work remains the complete multi-event
cold-replay traversal and its sufficient-present/reopening witness.

`TRACE-002` now passes at its finite two-event scope. A second ordinary event follows the first in
the authoritative ledger; both events independently replay through decode, support, binding, and
resumption after restart. Their source/return projections, unknown-causal traversal, one-class
current sufficient present, and event-sensitive reopen witness all regenerate without redispatch.
The next residual is a derived sufficient-present update/recovery view over a newly appended event;
it must preserve loss/reopening evidence without becoming mutable memory or causal history.

That update/recovery view now passes: it accepts only an extended exact history that preserves every
prior presentation/protected row, returns an updated derived present when the new event is
consequence-equivalent, and returns a positive reopen witness when it is not. The next residual is
now also closed at `SEPARATOR-001`: the exact positive reopen witness has a derived canonical
identity, enters the existing generic `SeparatorProblem`, retains its generated inquiry route and
complete admitted answer, and regenerates the same inspectable continuation after restart without
redispatch or a hard-coded method policy. `METHOD-BRIDGE-001` is also closed: one oriented,
content-addressed `MethodBridge` rechecks both `MethodContract`s, the declared residual, exact
answer-selected transport, guard, and reconstruction program; cold replay regenerates equal
reentry and a new protected reopen witness while rejecting undeclared residuals and rival
transports. The next residual is `CUE-PLANNING-001`: admit support, applicability, coverage, and
answer provenance for each exact finite cue before using the existing sufficient-basis/frontier
checker, preserving incomplete admission as `Unknown` and routing concrete ambiguity back through
the generic separator.

Do not let a provider or credential residual collapse transport actuality into semantic `Unknown`,
and do not predeclare support for an unpredictable content-addressed return. If the existing support
contract creates such a cycle, make that exact cycle the breaker and implement only the smallest
authority-preserving successor justified by the canonical specification and standing plan.

On every invocation, inspect Git actuality, `IMPLEMENTATION_FRONTIER.md`, `DECISIONS.jsonl`, and
`CONFORMANCE_STATUS.md`. Continue from the strongest newly demonstrated residual rather than
recreating a settled bridge.

---

# 3. Governing autonomous recurrence

Use the full protocol in `AGENTS.md`.

At project level, the recurrence is:

```text
RECONSTRUCT SUFFICIENT PRESENT
-> IDENTIFY STRONGEST LIVE OBLIGATION
-> BUILD SOFTWARE FRAME
-> MAP OCCUPANTS AND ARRANGEMENT
-> TRACE CONSEQUENTIAL RELATIONAL PATHS
-> CONSTRUCT MAXIMAL CLEAR CONTRAST
-> SUBTRACT TO DECISIVE FIXTURE
-> SHAPE IMPLEMENTATION HOLE
-> LEVERAGE NATIVE SOFTWARE METHOD
-> GENERATE SMALLEST REVERSIBLE SUCCESSOR
-> SEAL PREDICTION
-> ACTUALIZE
-> RETURN THROUGH SAME RELATION
-> SCRUTINIZE SUCCESS OR FAILURE
-> LOCALIZE EARLIEST DEFECT
-> REPAIR OR RETAIN
-> ATTACK WITH A STRONGER BREAKER
-> ABLATE REDUNDANT STRUCTURE
-> VERIFY REGENERATION / REOPENING
-> UPDATE PROJECT STATE
-> RECENTER ON NEW RESIDUAL
-> RECUR
```

The next task is generated by the residual. Do not wait for the user to hand-select each step.

---

# 4. Software-native perceptual reasoning aperture

Treat the repository as a **manipulable relational environment**, not primarily as a collection of files and text.

For every live obligation, reason through the following aperture.

## 4.1 Frame

Ask first:

```text
What frame makes this obligation meaningful?
```

Possible software frames include:

```text
semantic contract
type/interface boundary
crate/module boundary
source-program boundary
compiler/lowering boundary
runtime/effect boundary
persistence/transaction boundary
actuality boundary
decoder/resolution boundary
support/warrant boundary
binding/version boundary
replay/history boundary
method/applicability boundary
fold/recovery boundary
resource/performance boundary
```

The frame is itself provisional.

If multiple frames explain the same observation but imply different fixes, retain the family and construct a separator.

Do not let the task wording silently choose the ontology.

## 4.2 Possible occupants

Ask:

```text
What forms can lawfully occupy the roles in this frame?
```

Examples:

```text
type
form
relation schema
relation use
question
program
event
raw return
decoder
claim
support environment
standing result
method
fold
binding
transaction
test
function
module
database row
runtime state
```

Preserve:

```text
possible occupant
!= generated occupant
!= actual occupant
!= checked occupant
!= standing/accepted occupant
```

## 4.3 Actual occupants

Establish which forms actually occupy the relevant roles from repository actuality.

Do not infer actual occupancy from type compatibility or generation.

## 4.4 Arrangement

Ask:

```text
How are those occupants arranged?
```

Map relations such as:

```text
constructs
calls
depends-on
binds
owns
persists-before
decodes
checks
warrants
invalidates
resumes
branches-to
reopens
transports
composes
actualizes
replays
```

Preserve:

```text
same occupants != same arrangement
```

## 4.5 Relational separation

For each relevant pair/tuple, ask:

```text
Through what typed path does one affect the other?
How many transformations intervene?
Of what kinds?
Which intermediate roles carry authority, data, identity, timing, or effect?
```

Do not reduce this to file proximity or scalar graph distance.

Preserve:

```text
path length != relational separation
same endpoints != same path
same component states != same joint arrangement
```

A decomposition must preserve consequential joint relations, not merely list components.

## 4.6 Consequence

Ask:

```text
Which relation or path actually controls the protected observable?
```

Vary:

```text
presence
orientation
order
scope
applicability
coverage
authority
timing
binding
grain
composition
```

and observe which differences matter.

## 4.7 Maximal contrast, then subtraction

Construct an obviously sufficient or obviously failing extreme first.

Examples:

```text
force independent ingress vs rootless cycle
exact exhaustive coverage vs entirely unsearched
singleton return fiber vs deliberately plural protected return fiber
fully crash-safe persistence vs write-after-effect
stable historical binding vs replay under wrong version
```

Then remove detail until the smallest decisive discriminator remains.

Do not assume the smallest useful distinction before finding the axis.

## 4.8 Object-free relation

Once the defect is understood, strip away incidental names/files and express the software relation that must hold.

Examples:

```text
raw return must precede interpretation
support path must not depend on the conclusion it warrants
same use identity must determine the reverse return fiber
global guarantee requires local guarantees + explicit composition law
fold sufficiency requires protected factorization + recovery + unlock
```

Use the object-free relation to find existing project machinery or a native engineering method.

## 4.9 Native method

Ask:

```text
What software-engineering method natively constrains this relation?
```

Use, as appropriate:

```text
Rust type system / borrow checker
exact finite enumeration
property testing
state-machine/model exploration
database transaction/integrity tests
cold replay
differential testing
fuzzing
mutation/breaker tests
formal factorization/kernel check
profiling/resource measurement
compiler verification
```

The method is not authority beyond its contract.

## 4.10 Transform

Fill the relational hole with the smallest reversible change.

## 4.11 Return

Actualize through the compiler/test/runtime/database/replay path and preserve the raw return.

## 4.12 Determine

Ask:

```text
What in the patch actually caused the protected result?
What can be removed?
What failed to return?
What remains ambiguous?
```

## 4.13 Break

Attack both:

```text
failure -> sufficiency
success -> necessity
```

For a candidate implication `P -> Q`:

```text
sufficiency breaker: P and not Q
necessity breaker: not P and Q
```

## 4.14 Refold

When repeated structure is shown unnecessary at the active level, derive or fold it only if protected behavior, provenance, recovery, and reopening survive.

Then recenter on the next residual.

---

# 5. Development history as a sufficient present

The development process itself should instantiate the memory discipline being implemented.

Chronology is linear and append-only:

```text
Git commits
code evolution
test returns
DECISIONS.jsonl
FAILURES.jsonl
conformance evidence
```

The active engineering surface should not require replaying the entire history every time.

Use the current project state as a **development sufficient present**:

```text
canonical authority
+ implementation plan
+ IMPLEMENTATION_FRONTIER.md
+ CONFORMANCE_STATUS.md
+ applicable decisions/failures
+ current relevant code/tests
```

Older detail may leave the active reasoning window when its consequential role remains regenerable from repository ancestry.

Reopen ancestry when a live question makes a folded distinction consequential.

Do not infer that exact bounded active state is universally possible.

The research crawl has established the safer law:

```text
active size tracks irreducible protected width, not history length alone
```

A new horizon may force the active representation to grow.

---

# 6. Four distinct sufficient-present obligations

Do not collapse these:

```text
1. CONSEQUENCE SUFFICIENCY
   Does the present preserve the currently protected consequences?

2. RECURSIVE UPDATABILITY
   Can the successor present be computed from present + new event
   without replaying the full raw history?

3. REGENERATIVE RECOVERABILITY
   If a new question breaks a prior fold, can required ancestry be
   recovered or reacquired?

4. ACTIVE-RESOURCE POLICY
   Which recoverable distinctions should remain active under current
   memory/time/cost constraints?
```

Preserve:

```text
ConsequenceSufficiency
!= RecursiveUpdatability
!= RegenerativeRecoverability
!= BoundedActiveSize
```

Also preserve:

```text
ExactRecoverability != CheapLocalRecoverability
SmallestSemanticResidue != CheapestRecoveryRoute
SemanticSufficiency != FutureCostOptimalAperture
```

Do not claim an optimal active cache without an admitted request model or resource objective.

Context thrashing under unexpected future questions is a resource phenomenon, not automatically a semantic failure.

---

# 7. State as folded history

When current behavior is path-dependent, do not jump to either extreme:

```text
same visible state => same future
```

or:

```text
path dependence => full history must stay active
```

Instead ask:

```text
What recursively updateable state quotient retains the consequential history?
```

A later question that distinguishes histories currently mapped to one state is a separator on the state representation:

```text
same current representation
but
different protected future consequence
```

Then:

```text
separator
-> reopen/reacquire relevant ancestry
-> add/refine state coordinate
-> verify recursive update
-> refold
```

This is the later-phase `RefineFoldByQuestion` pattern, not a new primitive.

---

# 8. Autonomous method leverage

Do not merely work through the implementation plan as a static checklist.

Use already implemented mechanisms and tests to solve later obligations when lawful.

For every residual:

```text
What existing project method/contract already constrains this hole?
What external/native software method gives the strongest independent check?
```

When Phase 12/16 machinery becomes executable, method continuation should be residual-driven:

```text
M1
-> typed residual
-> relation/question
-> supported bridge/refinement
-> fill M2 requirement web
-> establish M2 applicability
-> execute M2
```

Never build the long-term controller as:

```text
if M1 fails -> call M2 by name
```

unless that is only the compiled representation of an explicit admitted bridge.

---

# 9. Current canonical reciprocal center remains binding

The newer general reasoning loop does **not** remove or weaken the positive-negation successor semantics.

When the obligation is reciprocal departure/return, implement the canonical sequence exactly:

```text
supported DeterminationPresentation
-> positive relevant non-circular DepartureWitness
-> admitted NegationUse
-> explicit semantic coverage
-> positive exterior
-> same-use reverse ReturnFiber
-> protected RecoveryCheck
-> seed/reorientation
-> second oriented NegationUse
-> second exterior
-> same-use return/recovery
-> residuals
-> Gamma downstream post-check
```

Do not replace this with generic “contrast” prose in the runtime.

The perceptual aperture governs how the implementation agent discovers and manipulates software obligations. The canonical reciprocal program governs the semantics of reciprocal inquiry in the calculus.

---

# 10. Progress rule

The default mode is autonomous continuation, not patch-and-wait. After each warranted local ratchet, derive the next live residual from the changed repository state and continue while the next action is authorized, evidence-bearing, and within the standing objective. Do not stop merely because one commit, fixture, or phase-local record is complete.


A recursion step counts as progress only when at least one of these changes:

```text
new actual return
new admitted separator/distinction
new verified capability
new supported representation/binding relation
repository state changed and was observed
known failure was structurally removed
live finite frontier strictly reduced
recovery/reopening capability improved and was demonstrated
```

A new explanation, new file, or different generated question alone is not progress.

---

# 11. Ratchet protocol

After each consequential verified change:

```text
1. inspect actual diff;
2. run smallest decisive fixture;
3. run affected gates;
4. attack the result with a neighboring breaker;
5. subtract redundant implementation;
6. cold-replay if history/authority semantics changed;
7. update CONFORMANCE_STATUS.md;
8. update DECISIONS.jsonl when a consequential local choice became accepted;
9. update FAILURES.jsonl when an actual failure constrains future work;
10. update IMPLEMENTATION_FRONTIER.md to the strongest remaining executable residual;
11. preserve Git/worktree ancestry according to repository policy;
12. reconstruct the successor development sufficient present;
13. continue automatically.
```

---

# 12. When to ask the user

Do not ask the user to babysit ordinary progression.

Ask only when:

```text
a genuine semantic/product authority choice is not encoded;
two protected-different successors remain and available evidence cannot separate them;
an external credential/permission/resource is required;
a destructive action outside established repository authority would be required.
```

When blocked, report the exact missing capability and continue all unaffected work.

---

# 13. Local and global stop states

Use the stop semantics in `AGENTS.md`.

Project-level successful completion requires:

```text
complete successor vertical chain executable;
required conformance fixtures passing at their declared coverage;
authoritative history cold-replayable;
no known unresolved semantic gap hidden by a declaration;
project state files reflect actual evidence;
remaining research gates are explicitly separated from required implementation.
```

Do not call the project complete because the known test suite is green if required semantic links are still declared rather than executed.

---

# 14. Compact standing command

```text
READ THE CURRENT AUTHORITIES.

RECONSTRUCT THE CURRENT SUFFICIENT PRESENT FROM REPOSITORY ACTUALITY.

READ THE IMPLEMENTATION FRONTIER.

DO NOT START WITH A PATCH.

ESTABLISH THE SOFTWARE FRAME.

ESTABLISH WHICH FORMS CAN OCCUPY IT AND WHICH ACTUALLY DO.

ESTABLISH THEIR ARRANGEMENT.

TRACE THE TYPED RELATIONAL PATH TO THE PROTECTED CONSEQUENCE.

CONSTRUCT A MAXIMAL CLEAR CONTRAST.

SUBTRACT TO THE DECISIVE FIXTURE.

LET THE DISTINCTION SHAPE THE IMPLEMENTATION HOLE.

USE THE STRONGEST NATIVE SOFTWARE METHOD THAT ACTUALLY CONSTRAINS THAT HOLE.

GENERATE THE SMALLEST REVERSIBLE SUCCESSOR.

SEAL THE PREDICTION.

ACTUALIZE THROUGH NOT-SELF.

PRESERVE THE RETURN.

CHECK IT THROUGH THE SAME RELATION.

SUCCESS STILL ATTACKS NECESSITY.
FAILURE ATTACKS SUFFICIENCY.

LOCALIZE THE EARLIEST DEFECT.

REPAIR THE FAILURE CLASS, NOT ONLY THE EXAMPLE.

PRESERVE PREDECESSOR CAPABILITY.

REMOVE REDUNDANT STRUCTURE.

RETAIN PROVENANCE, RECOVERY, AND REOPENING.

UPDATE THE IMPLEMENTATION FRONTIER FROM THE RESIDUAL.

RECENTER.

CONTINUE WITHOUT WAITING FOR ANOTHER TASK.

THE REPOSITORY HISTORY MAY GROW WITHOUT THE ACTIVE ENGINEERING SURFACE
GROWING IN PROPORTION, PROVIDED WHAT LEAVES THE ACTIVE SURFACE REMAINS
REGENERATIVELY AVAILABLE WHEN A FUTURE QUESTION MAKES IT CONSEQUENTIAL.

WORK UNTIL THE CURRENT CANONICAL INQUIRY CALCULUS IS A COMPLETE,
COLD-REPLAYABLE EXECUTABLE SYSTEM OR A LAWFUL PROJECT-LEVEL STOP IS REACHED.
```
