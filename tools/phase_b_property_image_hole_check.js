#!/usr/bin/env node
"use strict";const fs=require("node:fs"),path=require("node:path"),cp=require("node:child_process"),root=path.resolve(__dirname,".."),m=fs.readFileSync(path.join(root,"formal/InquiryCalculus/Legacy/V20/PropertyImageHole.lean"),"utf8"),need=["propertyImage","propertyForced","propertyImage_iff","selectedAnswer","literalFillerUniqueness","protectedClassUniqueness","forcednessWarrant","totalSolver","noProgramOrRustAuthority"];try{for(const n of need)if(!m.includes(n))throw Error(`missing:${n}`);if(!m.includes("∃ x, field x ∧ property x = value"))throw Error("image-witness");if(/\b(?:axiom|sorry|admit|unsafe|Program|Rust)\b/.test(m.replace(/\/-[\s\S]*?-\//g,"")))throw Error("escape");if(process.argv.includes("--compile"))cp.execFileSync("lake",["env","lean","InquiryCalculus/Legacy/V20/PropertyImageHole.lean"],{cwd:path.join(root,"formal"),stdio:"pipe"});console.log(`independent Phase B property-image-hole checks passed (${need.length}/${need.length}; compile ${process.argv.includes("--compile")?"checked":"delegated"})`)}catch(e){console.error(`Phase B property-image-hole check: ${e.message}`);process.exit(1)}
