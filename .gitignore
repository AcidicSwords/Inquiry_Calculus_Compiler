/target/
/formal/.lake/

# Local databases and SQLite sidecars
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Inquiry-ledger working evidence. The harness itself is committed; the traces
# are not, except when a DECISIONS.jsonl entry cites one as its evidence.
/.claude/trace/

# Rebuildable construction field; canonical inputs and independent returns are tracked.
/formal-successor/CONSTRUCTION_OBLIGATION_INDEX.json
/formal-successor/PROTECTED_CONSTRUCTION_SURFACE.json

# Local configuration and secrets
.env
.env.*
!.env.example
*.pem
*.key

# Editors and operating systems
.idea/
.vscode/
.DS_Store
Thumbs.db
